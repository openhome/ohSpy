<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="ohSpy.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:ohSpy.Core.ViewModels"
    xmlns:m="using:ohSpy.Core.Models"
    xmlns:conv="using:ohSpy.App.Converters"
    Title="ohSpy"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="ohSpy">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <!-- Story 5.2: the View menu (5.2 CREATES the app menu). The Network adapter submenu is populated
             on the MenuFlyout's Opening (the adapter list is runtime-variable, so the static x:Bind
             context-menu pattern from 2.8 won't serve; MenuFlyoutSubItem itself exposes no Opening event,
             so the rebuild hooks the parent MenuFlyout's Opening — open-Q #4: a title-bar command button +
             MenuFlyout, the cleaner WinUI-native fit). Story 5.1 (Diagnostics) and 5.3 (Rescan) will hang
             their items off this same View flyout when they land (they run later in Epic 5). -->
        <Button Grid.Row="1" Content="View" Margin="8,2" Padding="12,4">
            <Button.Flyout>
                <MenuFlyout Placement="BottomEdgeAlignedLeft" Opening="OnViewMenuOpening">
                    <MenuFlyoutSubItem x:Name="NetworkAdapterMenu" Text="Network adapter" />
                </MenuFlyout>
            </Button.Flyout>
        </Button>

        <!-- FR-001: Two-pane shell. Left: device tree. Right: SSDP log (Story 2.7 fills it). -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" MinWidth="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left pane: device tree (FR-001 + FR-002). NOTE: WinUI 3 TreeView databinding leaves
                 each generated TreeViewItem's DataContext null (confirmed via runtime inspection),
                 so the usual ItemContainerStyle {Binding Children} never resolves and NO chevrons /
                 children render (Story 2.5/2.6 gap, found in manual smoke). We therefore assign each
                 container's ItemsSource in code from the node's Children as containers realize
                 (OnTreeLayoutUpdated), which is the only mechanism that works here. OnTreeExpanding
                 drives the Story 2.6 lazy load; OnTreeDoubleTapped expands on double-click. -->
            <TreeView
                x:Name="DeviceTreeView"
                Grid.Column="0"
                ItemsSource="{x:Bind ViewModel.DeviceTree.Devices, Mode=OneWay}"
                SelectionMode="Single"
                Expanding="OnTreeExpanding"
                DoubleTapped="OnTreeDoubleTapped">

                <TreeView.Resources>
                    <conv:NodeDataTemplateSelector x:Key="NodeTemplateSelector">
                        <conv:NodeDataTemplateSelector.DeviceTemplate>
                            <DataTemplate x:DataType="vm:DeviceNodeViewModel">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <!-- Right-click context menu (FR-017 / FR-052): x:Bind to VM commands (Pattern 13) -->
                                    <Grid.ContextFlyout>
                                        <MenuFlyout>
                                            <MenuFlyoutItem Text="Fetch description XML"
                                                            Command="{x:Bind FetchXmlCommand}" />
                                            <MenuFlyoutItem Text="Properties&#x2026;"
                                                            Command="{x:Bind OpenPropertiesCommand}" />
                                        </MenuFlyout>
                                    </Grid.ContextFlyout>

                                    <!-- Kind glyph (FR-045): Segoe MDL2 Assets U+E703 "Network" -->
                                    <FontIcon
                                        Grid.Column="0" Grid.RowSpan="2"
                                        VerticalAlignment="Center"
                                        Margin="0,0,8,0"
                                        FontFamily="Segoe MDL2 Assets"
                                        FontSize="16"
                                        Glyph="{x:Bind KindGlyph}" />

                                    <!-- Primary text: FriendlyName (FR-009) -->
                                    <TextBlock
                                        Grid.Column="1" Grid.Row="0"
                                        Text="{x:Bind FriendlyName, Mode=OneWay}"
                                        TextTrimming="CharacterEllipsis" />

                                    <!-- Secondary detail: DeviceTypeTail · host:port (FR-051) -->
                                    <TextBlock
                                        Grid.Column="1" Grid.Row="1"
                                        Text="{x:Bind SecondaryDetail, Mode=OneWay}"
                                        FontSize="11"
                                        Foreground="{StaticResource MutedForegroundBrush}"
                                        TextTrimming="CharacterEllipsis" />
                                </Grid>
                            </DataTemplate>
                        </conv:NodeDataTemplateSelector.DeviceTemplate>

                        <conv:NodeDataTemplateSelector.ServiceTemplate>
                            <DataTemplate x:DataType="vm:ServiceNodeViewModel">
                                <StackPanel Orientation="Horizontal">
                                    <!-- Right-click context menu (FR-018 / FR-020): x:Bind to VM commands (Pattern 13) -->
                                    <StackPanel.ContextFlyout>
                                        <MenuFlyout>
                                            <MenuFlyoutItem Text="Fetch service XML"
                                                            Command="{x:Bind FetchServiceXmlCommand}" />
                                            <MenuFlyoutItem Text="Subscribe"
                                                            Command="{x:Bind SubscribeCommand}" />
                                        </MenuFlyout>
                                    </StackPanel.ContextFlyout>
                                    <FontIcon
                                        VerticalAlignment="Center" Margin="0,0,8,0"
                                        FontFamily="Segoe MDL2 Assets" FontSize="14"
                                        Glyph="{x:Bind KindGlyph}" />
                                    <TextBlock
                                        Text="{x:Bind Label, Mode=OneWay}"
                                        TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </DataTemplate>
                        </conv:NodeDataTemplateSelector.ServiceTemplate>

                        <conv:NodeDataTemplateSelector.ActionTemplate>
                            <DataTemplate x:DataType="vm:ActionNodeViewModel">
                                <StackPanel Orientation="Horizontal">
                                    <FontIcon
                                        VerticalAlignment="Center" Margin="0,0,8,0"
                                        FontFamily="Segoe MDL2 Assets" FontSize="14"
                                        Glyph="{x:Bind KindGlyph}" />
                                    <TextBlock
                                        Text="{x:Bind Label, Mode=OneWay}"
                                        TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </DataTemplate>
                        </conv:NodeDataTemplateSelector.ActionTemplate>

                        <conv:NodeDataTemplateSelector.FallbackTemplate>
                            <DataTemplate x:DataType="vm:INodeViewModel">
                                <TextBlock
                                    Text="{x:Bind Label, Mode=OneWay}"
                                    Foreground="{StaticResource MutedForegroundBrush}"
                                    FontStyle="Italic" />
                            </DataTemplate>
                        </conv:NodeDataTemplateSelector.FallbackTemplate>
                    </conv:NodeDataTemplateSelector>
                </TreeView.Resources>

                <!-- No ItemContainerStyle ItemsSource binding: the container DataContext is null
                     (see note above), so {Binding Children} resolves to nothing. Child ItemsSource
                     is assigned in code (OnTreeLayoutUpdated). -->
                <TreeView.ItemTemplate>
                    <DataTemplate x:DataType="vm:INodeViewModel">
                        <ContentControl
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            Content="{x:Bind}"
                            ContentTemplateSelector="{StaticResource NodeTemplateSelector}" />
                    </DataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <!-- Right pane: live SSDP log (FR-003 / FR-014 / FR-015 / FR-101) -->
            <Border
                Grid.Column="1"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                BorderThickness="1,0,0,0">
                <ScrollViewer
                    x:Name="LogScrollViewer"
                    VerticalScrollBarVisibility="Auto"
                    VerticalScrollMode="Enabled">
                    <ItemsRepeater ItemsSource="{x:Bind ViewModel.SsdpLog.Entries, Mode=OneWay}">
                        <ItemsRepeater.Layout>
                            <StackLayout Orientation="Vertical" />
                        </ItemsRepeater.Layout>
                        <ItemsRepeater.ItemTemplate>
                            <DataTemplate x:DataType="m:SsdpLogEntry">
                                <Grid Padding="8,2" ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <!-- Timestamp (local-time HH:mm:ss.fff) -->
                                    <TextBlock
                                        Grid.Column="0"
                                        Text="{x:Bind TimestampDisplay}"
                                        FontFamily="Consolas"
                                        Foreground="{StaticResource MutedForegroundBrush}" />
                                    <!-- ALIVE / BYEBYE literal token -->
                                    <TextBlock
                                        Grid.Column="1"
                                        Text="{x:Bind KindToken}"
                                        FontFamily="Consolas" />
                                    <!-- Device UUID -->
                                    <TextBlock
                                        Grid.Column="2"
                                        Text="{x:Bind UuidText}"
                                        FontFamily="Consolas"
                                        TextTrimming="CharacterEllipsis" />
                                </Grid>
                            </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Window>
