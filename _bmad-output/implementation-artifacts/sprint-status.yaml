# generated: 2026-06-01
# last_updated: 2026-06-01
# Story 1.1 context created via bmad-create-story 2026-06-01.
# project: ohSpy
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-06-01
last_updated: 2026-06-04 (Story 5.2 code-review APPROVED-WITH-MINOR-FIXES → D1 hardened + P1/P2 applied; committed at review; smoke = the verification keystone, Project Lead to run)
# 2026-06-04: Story 5.2 code-review (Sonnet, fresh context) APPROVED-WITH-MINOR-FIXES — A23 refactor confirmed regression-safe (RebindAsync hard-joins old loop before resetting _started; SetCallbackHost re-subscribe has no lost/dup/both window; DeviceRegistry.Clear correct); 503/2 across 3 runs. Follow-ups HANDLED by Opus: D1 (Project Lead chose "harden") → SwitchAdapterAsync catch disposes the partial scope unconditionally + nulls _adapterScope → coherent retryable "no active adapter" state (no auto-rebind) + regression test SwitchAdapterAsync_NewScopeStartFails_…; P1 removed the dead fetch-drain budget param/field/seam; P2 marked _callbackHost volatile + single-writer note. W1–W4 deferred (deferred-work.md). Verify: Core -warnaserror 0/0; full suite 504 passed/2 skipped (+1); switch suite green 3×. Story COMMITTED at `review` (Project Lead decision: "commit now, I'll smoke it"). ⚠️ MANUAL UI SMOKE (Task 6) STILL OPEN — the VERIFICATION KEYSTONE: on a multi-adapter machine, switch to the Linn-DS network and run the deferred 4.3 event-stream + 3.2(5/6/7) + 3.3(2/3/5) steps; supersedes Action-I. Mark 5.2 + the bundled deferred items `done` only after that smoke passes → then Epic 4 closes.
# 2026-06-04: Story 5.2 context created via bmad-create-story (claude-opus-4-8[1m]); 5-2 backlog → ready-for-dev. Adapter switch — View → Network adapter menu + FR-050 atomic rebind. The re-sequenced LAST story of Epic 4 (epic-4 already in-progress; not first story → no epic status change). THE single most interconnected story: tears down + rebuilds nearly every subsystem from Epics 2-4. KEY RECONCILIATIONS (D7/A23/epic prose vs SHIPPED code): (1) ⭐ A23 TRANSPORT-FACTORY PREREQ (Task 0, load-bearing) — ISsdpTransport is still a 2.1 DI SINGLETON; rebind needs per-adapter dispose+reconstruct → migrate to Func<ISsdpTransport> factory (chosen over a bespoke ISsdpTransportFactory; matches the existing Pattern-7 Func<> popup factories); AdapterScope must OWN+EXPOSE its transport so DiscoveryService reads the SCOPE-OWNED instance. BLAST RADIUS BIGGER THAN THE TRANSPORT: DiscoveryService is a singleton that ctor-injects the transport + has an Interlocked _started THROW-ON-SECOND-START guard + reads IncomingDatagrams → must be reconciled (recommended shape (b): keep singleton + RebindAsync(newTransport) that drains old loop, resets _started, starts fresh loop — keeps SsdpLogViewModel's AnnouncementReceived subscription stable; shape (a) per-adapter DiscoveryService is the alternative). OPEN-Q for impl: does EventCallbackHost.StartAsync run twice after DisposeAsync? if not (sockets/fields not reset, like the transport) it ALSO needs the factory treatment + re-thread ISubscriptionClient — this decision sizes Task 0. (2) ⭐⭐ DeviceRegistry.Clear() DOES NOT EXIST — epic AC + brief assumed it; only OnAlive/OnByebye/Remove/RemoveCore ship → Task 1 CREATES it (IDeviceRegistry + impl: UI-thread, cancel+dispose each DeviceCts + raise DeviceRemoved per UUID via the RemoveCore cascade, then empty the dict). SsdpLogViewModel.Clear() DOES exist (2.7 forward-compat) → separate call. (3) ADAPTER TYPE is NetworkAdapter (record Name/Description/IPv4), NOT AdapterCandidate (no such type) → SwitchAdapterAsync(NetworkAdapter). Enumerate()→IReadOnlyList<NetworkAdapter>. (4) _adapterCts lives in AdapterScope, NOT ShellViewModel → AdapterScope.DisposeAsync already does steps 1/2/7 (Cancel + transport dispose within the 2s WaitAsync budget + AdapterSwitchTimeout on overrun + _adapterCts.Dispose); SwitchAdapterAsync orchestrates steps 3 (host dispose) /4+6 (DeviceRegistry.Clear + SsdpLogViewModel.Clear) /5 (fetch drain) /8/9/10 (rebuild). AdapterScope.StartAsync currently always binds adapters[0] → must take the CHOSEN adapter. (5) VIEW MENU MUST BE CREATED HERE — MainWindow.xaml has NO menu bar at all (only TitleBar+tree+log); epic assumed 5.1 builds it first but 5.1/5.3 run LATER (still Epic 5) → 5.2 owns the View menu + Network adapter submenu (RadioMenuFlyoutItem per NetworkAdapterEnumerator.Enumerate(), current checked, 0/1-eligible→single disabled item; populate-on-Opening since the list is runtime-variable — the static x:Bind 2.8 ContextFlyout pattern won't work). 5.1/5.3 later hang Diagnostics/Rescan off it. (6) PER-POPUP FR-037 (verified): Properties (2.9) = DeviceRemoved-only (no CTS); Invocation (3.2) = _popupCts(DeviceToken)+DeviceRemoved; Subscription (4.3) = _popupCts(DeviceToken)+DeviceRemoved+handle.Lapsed(AdapterSwitch) (4.2 _adapterToken.Register(()=>Lapse(AdapterSwitch)) fires on step-1 cancel, off-thread, marshalled). 4.2 RE-CONTEXT: old token cancel lapses all subs BEFORE rebuild; SetAdapterContext(newScope.AdapterToken) at step 9 re-arms for FUTURE subs (None-default + CanBeCanceled guard). No popup blocks the 2s sequence (switch awaits its OWN work; popup transitions dispatched). (7) CONFIRMED PRE-EXISTING: DiagCategories.AdapterSwitch + AdapterSwitchTimeout (L93-98 → NO new constant, DiagCategoriesUsageTests exact-set unchanged); EventCallbackHost.DisposeAsync (IAsyncDisposable, budgeted 2s, 4.1); SubscriptionClient.SetAdapterContext; SsdpLogViewModel.Clear(). HEADLINE DESIGN: SwitchAdapterAsync in ShellViewModel; Interlocked re-entrancy guard (no concurrent switches; reject during startup; menu disabled while IsSwitching); 2s budget with force-tear-down+AdapterSwitchTimeout on drain overrun; reuse RunStartAsync's start block (don't duplicate); A26 fire-and-forget discipline; marshal post-await VM mutations (transient + menu-state) via IUiDispatcher.Post, DeferredUiDispatcher-guarded (Action H; InlineUiDispatcher masks it). TEST BOUNDARY: automated = Core 10-step order + 2s budget/timeout + DeviceRegistry.Clear removals + AC-7.1 cancellation drill (10 fetches→OCE<100ms, no post-to-disposed-VM) + popup-cascade VM + re-entrancy + SetAdapterContext re-call + marshalling guard, [Trait ac AC-7.x / AC-4.9]; App-only (CoreAppBoundary, no App test project) = View menu + RadioMenuFlyoutItem populate + LIVE rebind → MANUAL SMOKE (Task 6, first-class gate per smoke-per-ui-story). SMOKE = THE VERIFICATION KEYSTONE: 5.2 finally makes the Linn-DS network reachable in-app → its smoke BUNDLES the deferred 4.3 event-stream smoke (live NOTIFY+latest-values+lapsed banner) AND the deferred 3.2 (5/6/7) + 3.3 (2/3/5) steps; SUPERSEDES retro Action-I OHSPY_ADAPTER override (the real menu is Action-I's proper form → no throwaway override needed). Baseline 487 passed/2 skipped; expect a meaningful Core delta from the Task-0 refactor + switch tests. OPEN Qs flagged: EventCallbackHost re-start-after-dispose (sizes Task 0); DiscoveryService shape (a vs b); in-flight fetch drain handle (drainable join vs best-effort settle window); menu shape (MenuBar row vs title-bar MenuFlyout button).
# 2026-06-04: Story 4.3 code-review (Sonnet, fresh context) APPROVED-WITH-MINOR-FIXES — NO blockers, NO patches. Independently confirmed: marshalling complete (every handle-event/replay mutation via _ui.Post; the 4 DeferredUiDispatcher guards genuinely fail vs un-marshalled code), disposed-during-await guard race-free, concurrent-popup independence holds. 5 dev judgment calls reviewer-approved (incl. pin FeatureNotImplemented; #3 SID-vs-granted-TIMEOUT approved-with-reservation). 2 LOW coverage gaps + 1 trivial + 1 info → ALL handled by Opus: added DisposedDuringAwait_ClosesFreshHandle_NoAttach_NoLeak + ReplayBuffer_PreAttachLapse_IsDeliveredAndMarshalled tests; fixed the stale FeatureNotImplemented XML-doc (now documents prod-unused-but-pinned); logged the SubscriptionHandle granted-TIMEOUT seam gap to deferred-work.md. Verify: Core -warnaserror 0/0; full suite 487 passed/2 skipped (+2); 25-test popup suite green 3×. AUTOMATED SIDE = clean-APPROVED. ⚠️ STORY HELD AT `review` — Manual UI smoke (Task 12) is OPEN and CANNOT run without retro Action-I OHSPY_ADAPTER dev override (event-emitting Linn DS; Sky IGD emits nothing useful). Unlike 3.2/3.3 (partial smoke), ZERO of Epic 4's live-eventing payload is real-device-verified yet. DECISION PENDING (user): build Action-I now → smoke 4.3 + the deferred 3.2/3.3 steps on the Linn network → then `done`; OR accept-as-deferred → `done` with the entire event-smoke logged (Core VM is the compensating control). Not committed.
# 2026-06-04: Story 4.3 context created via bmad-create-story (claude-opus-4-8[1m]); 4-3 backlog → ready-for-dev. Subscription popup — the LAST GENA story + the FIRST/ONLY consumer of the 4.2 SubscriptionHandle seam. UI-touching (Core popup VM + App window) → manual UI smoke is a FIRST-CLASS gate (Task 12). KEY RECONCILIATIONS (epic/arch prose vs SHIPPED 4.1/4.2/2.7/2.9/3.2 code): (1) ⭐⭐ THE #1 HAZARD — SubscriptionHandle.NotificationReceived/Lapsed are RAW Action events that fire on the 4.2 NOTIFY-worker thread (thread-pool, NOT UI), AND the AC-4.2.7 replay buffer FLUSHES pre-attach events/lapse INSIDE the `add` accessor — i.e. on the thread that calls `add`, which for 4.3 is the OFF-THREAD post-await continuation of InitializeAsync (the VM attaches handlers after the SubscribeAsync await). So EVERY observable mutation in EVERY handler (Events.PrependNewest, LatestPropertyValues merge, Status/StatusMessage flips) MUST be marshalled via _ui.Post (winui-no-synccontext-marshal-vm; the 3.2 RPC_E_WRONGTHREAD crash class at its sharpest). _diag is thread-safe (emit may stay off-thread). 4.2's CloseAsync post-close patch (awaits worker → no event after close) protects the handler at teardown but does NOT relax normal-operation marshalling. Guard = a DeferredUiDispatcher regression test per handler (InlineUiDispatcher MASKS it — retro Action H), AC-4.3.12. (2) LAUNCH SEAM = ServiceNodeViewModel.SubscribeCommand, NOT the epic's ShellViewModel.OpenSubscriptionPopupCommand — the ServiceNode ALREADY holds _service+_parentEntry+_services, so the real body is just `_services.SubscriptionPopupLauncher.Open(_service, _parentEntry)` (mirrors ActionNode.OpenInvocationPopup; NO context threading, unlike 3.2's enriched ActionNode). (3) 4.2 SEAM SHAPES PINNED: ISubscriptionClient.SubscribeAsync(ServiceDescription, RegistryEntry, CancellationToken popupToken)→Task<SubscriptionHandle> (throws UpnpException on failed-subscribe → no SID, no UNSUBSCRIBE); SubscriptionHandle{Sid, NotificationReceived, Lapsed(SubscriptionLapseReason: RenewRefused/RenewTransportError/AdapterSwitch/DeviceGone), idempotent CloseAsync()}; EventNotification{Sid,Seq,ReceivedUtc,IReadOnlyDictionary<string,string> Properties} ALREADY PARSED (4.3 does NOT re-parse XML). 4.3 does NOT call SetAdapterContext (ShellViewModel already did). (4) REUSE verbatim: BoundedObservableCollection<EventNotification>(5000) [PrependNewest → Add(0)+Remove(cap), never Reset; 2.7 used 10K, epic pins 5K]; IInvocationPopupLauncher/InvocationPopupLauncher + WindowOwnershipManager Activate→Adopt (already multi-child via Dictionary<parentHwnd,List<childHwnd>>+GetChildrenOf → concurrent-popups AC needs NO ownership change); 2.9/3.2 DeviceRemoved banner (FR-037) + Interlocked Dispose. NodeServices gains an 8th member (ISubscriptionPopupLauncher) → same blast radius as the 3.2 7th-member add (MakeNodeServices + DeviceNode/DeviceTree/ActionNode test ctor sites + FakeSubscriptionPopupLauncher). (5) LatestPropertyValues shape = ObservableCollection<LatestPropertyValue{Name; [ObservableProperty] Value}> last-write-wins in-place (no ObservableDictionary in repo; BoundedObservableCollection is stream-only) — engineering judgment documented (epic L1691). (6) 2.8 SUBSCRIBE STUB REMOVAL: replace the Feature.NotImplemented Warning body + the "Subscribe (coming in Epic 4)" MainWindow.xaml label; retarget the ServiceNodeViewModelTests stub test. ⚠️ DiagCategories.FeatureNotImplemented becomes production-UNUSED (the 2.9 Properties stub already dropped it; Subscribe was its last consumer) BUT is architecturally PINNED by DiagCategoriesTests.ExactSet (name in expectedNames L64) → DECISION: LEAVE IT (do not touch DiagCategories.cs / its guard test; flag to reviewer as intentional known-unused-but-pinned, not an oversight). (7) MULTI-POPUP: each owns its own handle + _popupCts (linked to DeviceToken) + Events + LatestPropertyValues; FR-104 non-serial already isolated by 4.2's per-sub worker → VM just renders. (8) 5.2 (adapter switch, runs AFTER 4.3) cancels the adapter token → handle.Lapsed(AdapterSwitch) → 4.3's banner — build lapsed handling correctly NOW (it's 5.2's contract). TEST BOUNDARY: automated = Core SubscriptionPopupViewModel (subscribe wiring, marshalled append, latest-values merge, lapse-banner per reason, failed-subscribe, 5001-event eviction, dispose→CloseAsync, 5-concurrent + non-serial drill) + the DeferredUiDispatcher guard; App window/launcher/Activate→Adopt/right-click routing = App-only (CoreAppBoundary), MANUAL SMOKE. SMOKE PLAN (Task 12, retro Action E+J + memory): subscribe to an event-emitting Linn DS service (Ds/Product, Volume/Playlist) → events stream newest-first + latest-values update; 2nd concurrent popup on another service → independent; trigger lapse (power-off/leave) → banner; close mid-stream → clean UNSUBSCRIBE. ⚠️ DEPENDS ON retro Action I (OHSPY_ADAPTER dev override) to reach the Linn-DS network (Sky IGD likely emits no useful events); without Action I the event-stream steps are DEFERRED (like 3.2/3.3) with the Core VM logic unit-tested as the compensating control. Baseline 462 passed / 2 skipped; expect ~20-28 new Core tests. Open Qs for implementer: confirm InternalsVisibleTo lets a test drive SubscriptionHandle.RaiseNotification/RaiseLapsed directly (4.2 SubscriptionClientTests already drive internals → yes); LatestPropertyValue row ordering (append-on-first-seen, stable).
# 2026-06-04 (Story 4.2 DONE — both review patches applied + verified)
# 2026-06-04: Story 4.2 review patches APPLIED (Opus, main session) → 4-2 in-progress → done. Both Sonnet [Patch] findings fixed in SubscriptionClient.CloseAsync: (P2) after cancelling _loopCts + completing the channel, CloseAsync now awaits the renew loop then the NOTIFY worker via a bounded AwaitBoundedAsync(Task?) helper (DrainBudget=2s, swallows timeout/fault; EventCallbackHost.DisposeAsync WaitAsync precedent; #pragma VSTHRD003 for the own-task await) — so no RaiseNotification/RaiseLapsed can fire AFTER CloseAsync returns (protects Story 4.3's _ui.Post-marshalled handler from a post-teardown event — the 3.2 crash class); (P1) wasLapsed re-read moved to the latest point (after the loops drain, just before the UNSUBSCRIBE decision) so a concurrent device-gone/adapter-switch Lapse is observed → no UNSUBSCRIBE on an already-lapsed sub (window now near-zero). Verify: Core -warnaserror 0/0; full suite 462 passed / 2 skipped / 0 failed; SubscriptionClient 19-test suite green on 6 consecutive runs (no introduced flake). 2 DEFER items remain (the non-volatile _adapterToken barrier; SubscriptionClient has no DisposeAsync) → tagged Story 5.2, in deferred-work.md. No Patch-2 regression test (proving "no event after CloseAsync" needs a parse-delay seam that doesn't exist; existing close-path tests + 6× stability cover it). Not committed.
# 2026-06-04: Story 4.2 moved ready-for-dev → review by dev-story workflow (claude-opus-4-8[1m]). SubscriptionClient — the GENA lifecycle ORCHESTRATOR (headless Core, ohSpy.Core.Events). Created 5 Core files (EventNotification record [Models/], SubscriptionLapseReason enum, SubscriptionHandle [public, raw Action events + idempotent CloseAsync via close-delegate + a replay buffer that flushes pre-attach notifications to the first subscriber], ISubscriptionClient [public seam], SubscriptionClient [internal sealed: SID→subscription ConcurrentDictionary, per-sub bounded Channel+drain worker, per-sub renew loop, propertyset parse]). ORCHESTRATES the shipped 1.3 verbs (no SUBSCRIBE rebuild); 412 branched via UpnpTransportException.StatusCode==412; UnsubscribeAsync throws→CloseAsync swallows. CORRELATION by NotifyRequest.Sid (no path token v1); NOTIFY-before-SID race closed by a per-subscribe pending buffer drained at SID registration + a handle replay buffer (the latter closed a worker-vs-attach race that surfaced under full-suite load). PARSE BOUNDARY: 4.2 parses <e:propertyset> with the shared XXE-locked UpnpXmlReaderSettings.Create(); malformed→swallow one NOTIFY (Verbose, no lapse). AUTO-RENEW at min(0.8×granted, granted-30s) floored 1s, unit-tested WITHOUT real waits via an internal Func<TimeSpan,CT,Task> delay seam (EventCallbackHost internal-test-ctor precedent). D7: renew-loop CTS linked across popup+device+adapter; UNSUBSCRIBE-on-active-close uses a FRESH 5s CTS linked to _adapterToken ONLY (NOT the cancelled popup token) — asserted. Lifecycle/failure matrix all covered: failed-subscribe→no SID, no UNSUBSCRIBE, GenaSubscribeFailed; renew-fail→Lapsed(RenewRefused@412/RenewTransportError), no retry, lapsed-close sends no UNSUBSCRIBE, GenaRenewFailed; active-close→UNSUBSCRIBE swallow+GenaUnsubscribeFailed on fail; adapter-switch→Lapsed(AdapterSwitch)/device-gone→Lapsed(DeviceGone), no UNSUBSCRIBE; FR-104 non-serial (A 200ms parse, B observed <150ms); 3 concurrent subs, one 412-lapses, others route+renew fine. ISubscriptionClient singleton in ServiceRegistration (near IEventCallbackHost); ShellViewModel gains an ISubscriptionClient ctor arg + SetAdapterContext(scope.AdapterToken) right after _callbackHost.StartAsync (DI-resolved only, no new ShellViewModel test sites). 5.2 blast radius noted (re-SetAdapterContext + adapter-token cascade lapses all subs). NO new DiagCategories constant → DiagCategories.cs untouched, DiagCategoriesUsageTests UNCHANGED+green. Tests: extended StubUpnpHttpClient (controllable Subscribe/Renew/Unsubscribe responders + GenaCalls recording) + new FakeEventCallbackHost (settable CallbackBaseUrl + awaited RaiseNotifyAsync); 19 new tests, [Trait("ac","AC-4.2.x")]. GATES: Core 0/0; full suite 462 passed / 2 skipped / 0 failed (from 443/2; +19; 5× clean reruns, no flakiness); chaos 1; CoreAppBoundary+AsyncDiscipline+DiagCategoriesUsage green; App build 1 pre-existing benign WMC1506 (MainWindow.xaml:141), no new warnings. NO manual-UI-smoke (headless Core orchestration, no VM); real-device SUBSCRIBE+NOTIFY smoke is a 4.3 forward item. NOT committed. Recommend code-review (different LLM, fresh context). Open-Q resolutions in story Dev Agent Record (300s lease+80% margin; delay-func seam; SetAdapterContext seam; bounded-Channel per-sub worker; malformed→Verbose-swallow).
# 2026-06-04: Story 4.2 context created via bmad-create-story (claude-opus-4-8[1m]); 4-2 backlog → ready-for-dev. SubscriptionClient — the GENA lifecycle ORCHESTRATOR between the 4.1 callback host and the 4.3 popup. Headless Core (ohSpy.Core.Events), first consumer of BOTH the 1.3 GENA verbs AND the 4.1 NotifyReceived/CallbackBaseUrl seam. KEY RECONCILIATIONS (epic/arch prose vs SHIPPED code): (1) the 1.3 verbs ALREADY EXIST + are fully wired — IUpnpHttpClient.SubscribeAsync(eventSubUrl,callbackUrl,TimeSpan,ct)→SubscribeResponse(Sid,Timeout) [SID + granted lease pre-parsed from Second-N — do NOT re-parse], RenewSubscriptionAsync, UnsubscribeAsync [THROWS on failure per its doc-comment → 4.2's CloseAsync is the swallow layer]; a 412 renew-refusal surfaces as UpnpTransportException with StatusCode==412 (no dedicated type — branch on StatusCode); budgets GenaSubscribe/GenaUnsubscribe (5 s) pre-added + already consumed. 4.2 ORCHESTRATES, does not rebuild SUBSCRIBE. (2) the 4.1 seam is shipped: CallbackBaseUrl (Uri, passes straight into the verb's callbackUrl) + event Func<NotifyRequest,Task> NotifyReceived (host AWAITS handlers → 4.2's handler must return promptly; raw byte[] Body, never parsed by host). 4.1 open-Q#4 punted the callback-path-token call to 4.2. (3) ⭐ CORRELATION DESIGN (4.2 owns it): route NOTIFY by NotifyRequest.SID (ConcurrentDictionary<string,handle>), NOT a callback-path token — single shared CallbackBaseUrl, SID is the canonical GENA discriminator. The classic NOTIFY-before-SID race (first NOTIFY can arrive before the SUBSCRIBE response returns the SID) is closed by a per-subscribe pending-buffer/replay drained at SID registration — NOT a path token (path token recorded as a forward option only; 4.1 proved /sub/<tok> round-trips via PathAndQuery). (4) ⭐ PROPERTYSET-PARSE BOUNDARY: 4.1→raw bytes; 4.2 PARSES <e:propertyset> (FR-104, epic L1656 assigns it to the client, overriding arch L2295's VM sketch) using the shared UpnpXmlReaderSettings.Create() (1.4 XXE-locked); 4.3 only renders the parsed EventNotification (no re-parse — keeps 4.3 pure-UI per retro Action H). Malformed propertyset → swallow one NOTIFY (no lapse, no crash of host's awaited handler). (5) EventNotification.cs → Models/ (epic AC wins over arch source-tree's Events/); SubscriptionClient/Handle/LapseReason/ISubscriptionClient → Events/. (6) ALL SIX outbound Gena* constants pre-added (DiagCategories L55-69: Subscribe/SubscribeFailed/Unsubscribe/UnsubscribeFailed/RenewFailed) → NO new constant → DiagCategoriesUsageTests UNCHANGED; 4.2 emits the UUID-BEARING Gena* (it has parentEntry.Uuid) atop the verb-layer's uuid-less HttpTimeout/HttpTransport — the intentional-duplicate pattern Story 3.2 set for SoapFault. AUTO-RENEW: at ~80% of granted Timeout; NO injectable clock in Core → testable-without-real-waits via an internal Func<TimeSpan,CT,Task> delay seam (EventCallbackHost internal-test-ctor precedent) and/or a tiny-lease responder. LIFECYCLE/FAILURE baked as explicit ACs: failed-SUBSCRIBE reported (GenaSubscribeFailed) WITHOUT any UNSUBSCRIBE (no SID); renew-fail→stop(no retry)+Lapsed(RenewRefused@412/RenewTransportError)+GenaRenewFailed, mark lapsed → CloseAsync sends NO UNSUBSCRIBE; active-close→UNSUBSCRIBE via the D7 CLEANUP-USES-LEVEL-ABOVE-TOKEN invariant (fresh 5 s CTS linked to _adapterToken, NOT the cancelled popup token), failure swallowed+GenaUnsubscribeFailed; adapter-switch→Lapsed(AdapterSwitch)/device-gone→Lapsed(DeviceGone), no UNSUBSCRIBE; multiple subs concurrent+INDEPENDENT (thread-safe map, per-sub renew loops + bounded NOTIFY workers — one slow/failed renew or parse never blocks others, FR-104 non-serial). OWNERSHIP: ISubscriptionClient singleton (near IEventCallbackHost in ServiceRegistration); the DI-singleton can't inject the per-AdapterScope token → SetAdapterContext(scope.AdapterToken) called in ShellViewModel.RunStartAsync right after _callbackHost.StartAsync (ShellViewModel gains an ISubscriptionClient ctor arg — DI-resolved only, no test new-sites, same as 4.1). 5.2 blast radius noted (re-SetAdapterContext + adapter-token cascade lapses all subs). ⚠️ NO MANUAL UI SMOKE (headless Core orchestration, no VM/IUiDispatcher — 4.1/3.1 posture); retro Action H (DeferredUiDispatcher) folds into 4.3 not 4.2. Real-device SUBSCRIBE+NOTIFY smoke is a 4.3 forward item (needs popup + event-emitting Linn DS via Action-I dev override). TEST PLAN: controllable StubUpnpHttpClient (make Subscribe/Renew/Unsubscribe responders + record calls, mirror InvokeResponder) + a new FakeEventCallbackHost (settable CallbackBaseUrl + raise NotifyReceived) → happy lifecycle, auto-renew-fires+reschedules, renew-refused(412)/transport-fail→lapse(no retry), failed-subscribe-no-unsubscribe, active-close UNSUBSCRIBE (assert level-above token NOT cancelled), lapsed-close-no-unsubscribe, adapter/device cancel, NOTIFY-before-SID race, propertyset parse+malformed-swallow, FR-104 non-serial drill (A 200ms / B <50ms), concurrent-independent. Baseline 443 passed / 2 skipped. Open Qs flagged: initial-lease(300s)+renew-margin formula, renew-timing seam choice, SetAdapterContext vs read-off-host, per-sub bounded-worker shape, malformed-propertyset diagnostic level.
# 2026-06-04: Story 4.1 moved in-progress → review by dev-story workflow (claude-opus-4-8[1m]). Event Callback Host — the FIRST inbound network listener. Created src/ohSpy.Core/Events/ (7 source files: NotifyRequest [public record, raw byte[] Body], IEventCallbackHost [public iface], EventCallbackHost [internal sealed: TcpListener bound to the adapter IP + ephemeral port — NOT 0.0.0.0/IPAddress.Any, FR-049 no-Admin], HttpRequestParser [internal sealed hand-rolled HTTP/1.1, strict framing + lenient headers, emits a Success/Failure discriminated result], HttpRequestParseResult, TimeoutStream [internal sealed active-budget idle-read enforcer], CallbackTimeoutException [sentinel]). HARDENING (all tested): conn cap 8 via SemaphoreSlim non-blocking Wait(0) (9th → accept+close+GenaCallbackFlood); 5+5 s header/body idle budgets via TimeoutStream (slowloris); 16 KB header (413 Oversize) / 1 MB body cap (HttpRequestParser.MaxBodyBytes=1_048_576 — NOT MaxGenaResponseBytes; rejected by Content-Length BEFORE buffering, 413); Content-Length required (411 NoLength) + duplicate-CL strict 400 (not last-wins); chunked 400; bare-CR/three-SP/lowercase-method/obsolete-fold 400 Malformed; bare-LF accepted; known-header last-wins + case-insensitive + ignore-unknowns (64 cap); SEQ absent/unparseable→0; PathAndQuery verbatim; every Gena.Callback.* Warning carries RemoteEndpoint, DeviceUuid null; budgeted idempotent DisposeAsync (2 s drain, FR-050-aligned for 5.2 atomic rebind; force-close+log on overrun; safe no-op when never started). Fully async, ConfigureAwait(false), private CTS linked to adapter token; no .Result/.Wait. NO new HttpTimeoutOptions, NO new DiagCategories constant → DiagCategoriesUsageTests UNCHANGED. WIRING (open Q#2): IEventCallbackHost singleton in ServiceRegistration (lifecycle owned by ShellViewModel/AdapterScope, NOT DI-autostarted); ShellViewModel gains an IEventCallbackHost ctor arg, StartAsync(scope.CurrentAdapterIPv4, scope.AdapterToken) in RunStartAsync at the bound-IP gate (before discovery), DisposeAsync after the scope. No ShellViewModel test ctor sites broke (DI-resolved only). 5.2 blast radius: reach the host via ShellViewModel (or relocate into AdapterScope then). OPEN Q#1 (reviewer): the 500 internal-dispatch-error + the rare drain-exceeded warnings reuse GenaCallbackMalformed / GenaCallbackFlood respectively (no dedicated constant in the pre-added set; adding one would break the no-new-constant reconciliation) — confirm acceptable. TESTS: hand-rolled FakeGenaClient (raw TcpClient) + ConcurrentCapturingDiagnosticEmitter; SlowlorisTest (8 drip→8× HeadersTo, 9th 200) + FloodTest (50→≤8 in-flight + Flood + no leak) on shrunk ms budgets; +47 Events tests. GATES: Core 0/0; full suite 443 passed / 2 skipped / 0 failed (from 396/2); chaos 1; CoreAppBoundary+AsyncDiscipline+DiagCategoriesUsage green; App build 1 pre-existing benign WMC1506 (MainWindow.xaml:141), no new warnings. NO manual-UI-smoke gate (headless Core, no VM — Story 3.1 posture); real-device NOTIFY smoke is a 4.2/4.3 forward item. NOT committed. Recommend code-review (different LLM, fresh context).
# 2026-06-04: Story 4.1 context created via bmad-create-story (claude-opus-4-8[1m]); Epic 4 OPENS (epic-4 backlog → in-progress); 4-1 backlog → ready-for-dev. Event Callback Host — TcpListener + hand-rolled HTTP/1.1 parser (the FIRST inbound listener; headless Core/infra, SsdpTransport-shaped, NOT UI). KEY RECONCILIATIONS (Decision 4 + epic prose vs SHIPPED code): (1) src/ohSpy.Core/Events/ does NOT exist yet — this story creates the folder + all 4 files (NotifyRequest.cs, IEventCallbackHost.cs, TimeoutStream.cs, HttpRequestParser.cs, EventCallbackHost.cs), namespace ohSpy.Core.Events. (2) PRE-ADDED SCAFFOLDING CONFIRMED + cited: HttpTimeoutOptions.CallbackHeaders + CallbackBody (5 s each, L22-24) — use them, don't invent; ALL DiagCategories.Gena.Callback.* constants (Malformed/Oversize/NoLength/HeadersTo/BodyTo/Flood + GenaNotifyReceived, L71-91) pre-added → NO new DiagCategories constant → DiagCategoriesUsageTests (reflection-based) UNCHANGED. Each hardening path mapped to its existing constant in an AC matrix. ⚠️ MaxGenaResponseBytes (64 KB) is the OUTBOUND response cap (4.2) — do NOT reuse for the inbound 1 MB body cap (D4 constant, hard-code). (3) ADAPTER-IP SEAM: AdapterScope.CurrentAdapterIPv4 (IPAddress?, set after transport bind) + AdapterToken; the host binds TcpListener(IPEndPoint(adapterIPv4, 0)) ephemeral port — NOT 0.0.0.0 (FR-049 no-Admin, no URL ACL). Start point = ShellViewModel.RunStartAsync where `scope.CurrentAdapterIPv4 is not null` (same gate that starts DiscoveryService); host registered as a singleton (ISsdpTransport precedent) whose Start/Dispose lifecycle is scope-owned, NOT DI-autostarted → ShellViewModel gains an IEventCallbackHost ctor arg (blast radius: DI L140 + ShellViewModel test ctor sites). Open Q: ShellViewModel vs AdapterScope wiring (5.2 atomic-rebind prefers AdapterScope, but it's internal + has no host field today). (4) CallbackBaseUrl (Uri) → Story 4.2: IUpnpHttpClient.SubscribeAsync(Uri eventSubUrl, Uri callbackUrl, TimeSpan, CT) signature CONFIRMED (L46-47) — host exposes Uri in the exact shape 4.2 consumes. (5) NotifyReceived(NotifyRequest{Sid,Seq,PathAndQuery,Body bytes,ReceivedUtc}) is the RAW hand-off — host does NOT parse <e:propertyset> (D4 L464); 4.2 SubscriptionClient filters by SID + parses body, 4.3 popup renders. HARDENING BAKED IN (D4): conn cap 8 (SemaphoreSlim, 9th accept+close+Flood), 5+5 s header/body budgets via TimeoutStream (slowloris), 16 KB header / 1 MB body caps (Oversize, reject body by Content-Length BEFORE buffering), Content-Length required (411 NoLength) + duplicate-CL 400, chunked 400, strict request-line (2 SP, uppercase method, bare-CR reject/bare-LF accept), obsolete-fold 400, lenient header case + last-wins known headers + ignore unknowns (64 cap); every Warning carries RemoteEndpoint (DeviceUuid unknown at host layer); idempotent budgeted DisposeAsync (2 s drain, FR-050-aligned — Story 5.2 atomic rebind calls it). Fully async (no .Result/.Wait — AsyncDisciplineTests/VSTHRD gates), ConfigureAwait(false), linked CTS to adapter token. TEST PLAN: hand-rolled FakeGenaClient (raw TcpClient) drives every AC in-process + SlowlorisTest (8 drip conns → 5 s headers TO, 9th opens after slot frees) + FloodTest (50 conns → 8 served, 42 Flood) — shrink CallbackHeaders/Body to ms via Configure<HttpTimeoutOptions> for fast timeout tests; reuse Fakes/HangingStream.cs for TimeoutStream units. ⚠️ NO MANUAL UI SMOKE (headless Core, no VM — Story 3.1 posture); real-device NOTIFY smoke is a FORWARD item for 4.2/4.3 (needs SUBSCRIBE + event-emitting device — Linn DS; reachable via retro Action-I dev adapter override). Retro Action H (DeferredUiDispatcher) folds into 4.3 not 4.1. Open Q: the internal-dispatch-error (500) path has NO dedicated Gena.Callback.* constant in the pre-added set (D4 says "Warning with stack") — recommend NOT adding one (would break the no-new-constant reconciliation); reviewer to confirm. Sized 1.5× (architecture risk carry-forward, epic L1499). Baseline 396 passed / 2 skipped.
# 2026-06-04: Epic 3 (Action Invocation) CLOSED — 3/3 stories done+reviewed+committed (dfa5b81, 0c11c8b, 2a29e84); epic-3 in-progress → done; epic-3-retrospective optional → done (epic-3-retro-2026-06-04.md). Retro headline: all 3 Epic 2 action items (E smoke-per-UI, F prove-it's-wired, G record-WinUI-quirks) were APPLIED and each earned its keep — smoke caught the 3.2 RPC_E_WRONGTHREAD crash that 356 green tests missed (InlineUiDispatcher ran Post inline and masked the missing _ui.Post marshalling). Action items: H = one DeferredUiDispatcher marshalling-guard test per async VM path (keep InlineUiDispatcher elsewhere) — bake into 4.3 create-story; I = dev adapter override (env var) as Epic 4 kickoff prep to unblock Linn-DS smoke; J = front-load the GENA event-smoke plan in create-story (identify which live devices emit events). DECISION: Story 5.2 (adapter switch) re-sequenced Epic 5 → end of Epic 4 (forward dep on 4.1 EventCallbackHost + 4.3 subscription popup; prereq A23 transport factory). Standing verification debt: deferred 3.2 (5/6/7) + 3.3 (2/3/5) UI smokes — revisit via the dev override / 5.2. Next: Epic 4 (start 4.1 callback host) after the kickoff prep.
# 2026-06-04: Story 3.3 MANUAL SMOKE (live Sky network) — DROPDOWN (FR-102) PASS: an enumerated <allowedValueList> action rendered a ComboBox of exactly those values, pre-selected; selecting + invoking sent the chosen value to the device (selector→ComboBox→SelectedValue→ResolvedValue→SoapArgument end-to-end confirmed). Steps DEFERRED for lack of suitable actions on the Sky IGD: 2 numeric NumberBox (FR-103), 3 off-step rejection, 4 fallback-to-text, 5 loading state — the network has no <allowedValueRange> numeric action; the Linn DS devices that do are on another network reachable only once the Story 5.2 adapter switch lands. Each deferred step's Core VM logic IS unit-tested (AllowedValueRangeArgumentViewModel Min/Max/Step + invariant ResolvedValue; off-step pre-flight gate + ValidationError; ResolveInput→base fallback; IsLoadingInputs marshalled true→false) — only the App-side template/projection is unverified (NumberBox binding, inline-error TextBlock, Loading visibility), and the fallback TextBox is unchanged 3.2 behaviour already smoke-passed → low, bounded risk. Honest partial recorded in story Task 9. Proceeding to code-review (Sonnet, fresh context); `done` will carry the deferred-2/3/4/5 caveat (revisit on a Linn-DS network / Story 5.2).
# 2026-06-03: Story 3.3 dev-story (claude-opus-4-8[1m]) — automated portion COMPLETE; 3-3 ready-for-dev → review. Shipped: sealed AllowedValueListArgumentViewModel (FR-102) + AllowedValueRangeArgumentViewModel (FR-103, invariant ResolvedValue, off-step ValidationError); InvocationPopupViewModel +IScpdParser (8th ctor arg) + InitializeAsync (fetch SCPD → ReadStateTableAsync → rebuild Inputs with variants, ALL post-await mutation marshalled via _ui.Post, ConfigureAwait(false)) + ResolveInput resolver + EmitScpdParse + off-step Invoke pre-flight gate; App ArgumentInputTemplateSelector (ComboBox/NumberBox/TextBox) + "Loading…" hint + launcher fire-and-forget InitializeAsync kick-off; DI factory grows IScpdParser. ⚠️ The 3.2 smoke-crash marshalling discipline (winui-no-synccontext-marshal-vm) is honoured and GUARDED by a DeferredUiDispatcher regression test (InitializeAsync_MarshalsRebuildThroughDispatcher_NotDirectly). Open-Q resolutions: ResolveInput=private VM method; off-step=inline ValidationError+short-circuit; per-service caching=DEFERRED; numeric set=ui1/ui2/ui4/i1/i2/i4/int (unwidened). GATES: Core build 0/0; solution build 1 warning (pre-existing WMC1506 only); full suite 396 passed / 2 skipped (+40 from 356); chaos unchanged; CoreAppBoundary+AsyncDiscipline+DiagCategoriesUsage green; no new DiagCategories constant. ⚠️⚠️ MANUAL UI SMOKE (Task 9) IS OPEN — a genuine blocker to `done`, NOT run by the dev agent (headless, no device, no display). Human must run: SetMute ComboBox, SetVolume 0..100 step-1 NumberBox + invariant wire value, off-step rejection (no SOAP fires), fallback-to-text, Loading state. Minor reviewer notes: broad NFR-R3 catch in InitializeAsync; inline-error TextBlock binds ValidationError directly (no converter — a converter's StaticResource lookup root must be a FrameworkElement, unavailable under the Window-rooted x:Bind). NEXT: code-review (fresh context, different LLM), then smoke, then done.
# 2026-06-03: Story 3.3 context created via bmad-create-story (claude-opus-4-8[1m]); 3-3 backlog → ready-for-dev. Constrained inputs: <allowedValueList> dropdown + <allowedValueRange> numeric — the LAST Epic 3 story, layered onto 3.2's invocation popup. KEY RECONCILIATIONS (epic text vs shipped reality): (1) IScpdParser.ReadStateTableAsync ALREADY EXISTS + is fully implemented (Story 1.4, AC-5.5) — parses allowedValueList (empty→empty list), allowedValueRange (null Step when omitted), defaultValue, dataType into ScpdStateTable.ByName; models (ScpdStateTable/ScpdStateVariable/ScpdAllowedValueRange/ScpdArgument.RelatedStateVariable) all final. NO parser/model work — consumer-side resolution only. (2) ⚠️ ASYNC INPUTS LOAD: the 3.2 popup builds Inputs SYNCHRONOUSLY in the ctor; a ctor can't await the state-table fetch → this story adds a public async Task InitializeAsync() the LAUNCHER kicks off after Activate(); ctor keeps text-only Inputs (fallback) + IsLoadingInputs=true; InitializeAsync fetches+parses then REBUILDS Inputs with resolved variants. (3) ⚠️⚠️ THE BIG ONE — the just-fixed 3.2 smoke CRASH (winui-no-synccontext-marshal-vm): WinUI 3 has NO SynchronizationContext so the post-await continuation runs OFF the UI thread → EVERY mutation of Inputs/IsLoadingInputs MUST be marshalled via _ui.Post (Decision 1), exactly as InvokeAsync now does; tests MUST use DeferredUiDispatcher (InlineUiDispatcher masks missing marshalling — the exact reason 3.2's crash slipped CI). Made this a front-and-centre Dev Note + AC-3.3.1 + Task 3 + a marshalling regression test. (4) SCPD BYTES SOURCE: ServiceNodeViewModel.LoadActionsAsync DISCARDS the SCPD bytes (streams actions, never retains byte[]); ServiceDescription is an immutable record; the launcher seam doesn't pass the node → popup VM RE-FETCHES via _http.FetchScpdAsync(new Uri(LocationUrl, ScpdUrl), token) (the LoadActionsAsync idiom). Spec-blessed (arch L597: re-parse ~50ms acceptable). Per-node table caching DEFERRED (Open Q#3 — would need re-plumbing the seam). DESIGN DECISIONS BAKED IN: sealed subclasses AllowedValueListArgumentViewModel (IReadOnlyList<string> AllowedValues + [ObservableProperty] SelectedValue, override ResolvedValue=>SelectedValue) + AllowedValueRangeArgumentViewModel (Minimum/Maximum/Step + [ObservableProperty] NumericValue, override ResolvedValue=>NumericValue.ToString(InvariantCulture) FR-103) over a kind-discriminator (the 3.2 base was deliberately shaped non-sealed+virtual ResolvedValue for this); off-step validation = inline ValidationError + InvokeAsync pre-flight short-circuit (not CanExecute); App heterogeneous rendering = a DataTemplateSelector keyed on VM runtime subtype (mirror NodeDataTemplateSelector — a DELIBERATE divergence from 3.2's result-area code-behind Visibility projections, justified by list-of-heterogeneous-items vs one-of-three singleton panels); ComboBox+NumberBox WinUI-native. FALLBACKS pinned as ACs: state-table fetch/parse fails→all-text + Warning ScpdParse; list empty / range-on-non-numeric / min>max / step≤0→that-arg-to-text + ScpdParse; both list+range→FR-102 wins + ScpdParse; neither→text (no diag, PRD §7 Non-Goal no dataType-driven typed inputs in v1). DiagCategories.ScpdParse ALREADY exists (no new constant → DiagCategoriesUsageTests unchanged). TEST BOUNDARY: automated = the 2 constrained VMs + InvocationPopupViewModel.InitializeAsync variant-resolution/fallbacks/marshalling (DeferredUiDispatcher guard); App XAML templates/selector/NumberBox = App-only, UNTESTABLE (CoreAppBoundary), MANUAL SMOKE (Task 9, first-class gate before review/done per Epic 2 retro action E + memory smoke-per-ui-story — SetMute dropdown, SetVolume 0..100 step1 numeric, off-step rejection, fallback-to-text). Fakes to extend: StubScpdParser.ReadStateTableAsync (currently throws NotSupportedException → make controllable), StubUpnpHttpClient.ScpdResponder (reuse). Blast radius: InvocationPopupViewModel gains an 8th ctor arg (IScpdParser) → DI factory + InvocationPopupViewModelTests setup update (launcher seam signature UNCHANGED). ~18-24 new Core tests (baseline 356/2). Existing fixture state-table-rich.xml covers Mute/Volume(range step1)/Balance(range no-step)/Mode(list). Open Qs: ResolveInput location (private VM method recommended), off-step UX, per-service caching (deferred), numeric dataType set (ui1/ui2/ui4/i1/i2/i4/int per epic).
# 2026-06-03 (prior): Story 3.2 review → done — code-review APPROVED-WITH-MINOR-FIXES; 2 patches applied
# 2026-06-03: Story 3.2 MANUAL SMOKE re-run on live Sky network — CORE PATHS PASS. Steps PASSED: 1 open ≤1s (SC-010); 2 argument-less success (GetConnectionTypeInfo) — also surfaced+fixed a cosmetic arg-name clip (160px hard-clip of NewPossibleConnectionTypes → widened to 200px + TextWrapping=Wrap in InvocationPopupWindow.xaml); 3 typed-inputs-reach-device (GetGenericPortMappingEntry NewPortMappingIndex); 4 UPnP fault + caution styling (out-of-range index → HTTP 500 713 SpecifiedArrayIndexInvalid); 8 FR-046 ownership (above/stays-above/minimise/close-with-main). Steps DEFERRED to a richer network: 5 transport-error styling, 6 device-gone banner, 7 close-mid-invoke — single-device Sky LAN can't cleanly force them; EACH has passing Core VM unit tests (Invoke_Transport_/Invoke_Protocol_, DeviceRemoved_UuidMatch_FlipsBanner, Dispose_MidInvoke_CancelsSwallowsOce_), so only the App-side Visibility projection for those two variants is unverified — the SAME code-behind projection mechanism the PASSED fault(4)+ownership(8) steps validated → low, bounded risk. Honest partial, NOT a silent defer (recorded in story Task 10). App build 0/0 after the XAML clip fix. Not committed. Smoke gate satisfied for the core paths; proceeding to code-review (Sonnet, fresh context). Revisit 5/6/7 when multi-device/Linn-DS network or Story 5.2 adapter switch is available.
# 2026-06-03: Story 3.2 MANUAL SMOKE (live Sky Hub) found a CRASH on step 2 (invoke argument-less action): "Invoking…" spinner froze ~1-2s then the app exited. Root cause via Windows event log (.NET Runtime 1026): System.Runtime.InteropServices.COMException 0x8001010E (RPC_E_WRONGTHREAD) at UIElement.set_Visibility ← InvocationPopupWindow.Update_InvokingVisibility ← VM set_IsInvoking ← InvokeAsync finally (the post-await continuation). WinUI 3 installs NO SynchronizationContext, so `await InvokeActionAsync(...).ConfigureAwait(true)` resumed on a THREAD-POOL thread; setting Result/IsInvoking there raised PropertyChanged off-thread → bound window poked UIElement.Visibility off-thread → unhandled crash (no diagnostic — it's in the XAML layer, not the emitter). FIX (InvocationPopupViewModel.InvokeAsync): ConfigureAwait(false) + marshal the terminal state (Result + IsInvoking=false) and the OCE-path IsInvoking=false via _ui.Post(...) — the IUiDispatcher seam (Decision 1), mirroring ServiceNodeViewModel. Pre-await mutations stay direct (RelayCommand body runs on the UI thread). Diagnostics stay off-thread (_diag is thread-safe). WHY CI MISSED IT: the VM tests drove through InlineUiDispatcher, whose Post runs inline and masks missing marshalling (Epic 2 "prove it's wired" lesson AGAIN). Added DeferredUiDispatcher fake (queues, runs on Drain) + regression test Invoke_Success_MarshalsTerminalStateThroughDispatcher_NotDirectly_AC327 (asserts Result still null + IsInvoking still true until Drain → proves it went through Post). Recorded durable knowledge: memory winui-no-synccontext-marshal-vm (applies to every async popup VM — 3.3/4.3/5.x). Core suite 355→356 passed / 2 skipped / 0 failed; chaos=1; boundary+async+diag guards green. App build 0 errors / 1 pre-existing WMC1506. NOT committed (joins the uncommitted 3.2 batch). Story stays at `review`, smoke gate OPEN — human to RESTART the 8-step smoke from step 1 on a live device. (Fresh-context dev-story implementation otherwise unchanged.)
# 2026-06-03: Story 3.2 moved ready-for-dev → in-progress → review by dev-story workflow (claude-opus-4-8[1m]). Invocation popup with free-form text inputs — first consumer of Story 3.1's SOAP layer + second reuse of Story 2.9's WindowOwnershipManager/Activate→Adopt popup pattern. Delivered (automated surface, all gates green): ArgumentInputViewModel (Core, non-sealed text-only base, virtual ResolvedValue=>Value seam for Story 3.3), InvocationResultViewModel (abstract record + Success/Fault/TransportError sealed variants, reuses SoapArgument), IInvocationPopupLauncher (Core seam) + InvocationPopupLauncher (App impl, named InvocationPopupViewModelFactory delegate, dual-reg, ShellWindow set in App.OnLaunched, Activate→Adopt), InvocationPopupViewModel (Core sealed IDisposable — guarded Uri.TryCreate(LocationUrl,ControlUrl) resolves the relative-control-URL reconciliation #1, _popupCts linked to PUBLIC RegistryEntry.DeviceToken, DeviceRemoved banner, UUID-bearing Pattern-11 SoapFault/SoapInvoke/HttpTimeout emits [reconciliation #4 — KEPT the popup-level emit over the uuid-less 3.1 http-layer one], OCE swallowed, Interlocked dispose cancel→unsubscribe→dispose), enriched ActionNodeViewModel (+ServiceDescription/RegistryEntry/NodeServices ctor + OpenInvocationPopupCommand) with RegistryEntry threaded DeviceNode→ServiceNode→ActionNode, NodeServices 7th member + DI, InvocationPopupWindow.xaml/.cs (per-type Visibility projections in code-behind — open Q#1; mirror PropertiesWindow), MainWindow.OnTreeDoubleTapped action branch. Open questions resolved: (1) per-type Visibility projections not DataTemplateSelector; (2) Title="{serviceTail} · action" reusing :service: tail; (3) named factory delegate; (4) argument-less ResolvedValue="" confirmed, no special-casing. Blast radius handled: StubUpnpHttpClient gained InvokeResponder+InvokedRequests; FakeInvocationPopupLauncher added; 4 NodeServices ctor sites + 1 DI reg + ServiceNodeViewModel/ActionNodeViewModel ctor sites updated. +25 Core tests (330→355 passed / 2 skipped / 0 failed); chaos=1; CoreAppBoundary(4)+AsyncDiscipline+DiagCategoriesUsage green (no new DiagCategories constant — reused HttpTimeout/SoapFault/SoapInvoke). Core build 0/0; App clean -t:Rebuild = 1 pre-existing benign WMC1506 (MainWindow.xaml:141, Story 2.5 FallbackTemplate), no new warnings. ⚠️ TASK 10 MANUAL UI SMOKE NOT EXECUTED — headless env, no real UPnP device. SMOKE GATE REMAINS OPEN: this is a genuine blocker to `done` (Epic 2 retro action E + project memory smoke-per-ui-story). Concrete smoke steps recorded in the story's Completion Notes (open ≤1s SC-010, success ≤2s SC-011, fault/transport styling, device-gone banner, close-mid-invoke, FR-046 ownership). Code-review may proceed; human MUST run the smoke on a live device before review→done. Recommend code-review with a different LLM.
# 2026-06-03: Story 3.2 context created via bmad-create-story (claude-opus-4-8[1m]); 3-2 backlog → ready-for-dev. Invocation popup with free-form text inputs — the first consumer of Story 3.1's SOAP layer + the second reuse of Story 2.9's WindowOwnershipManager/Activate→Adopt popup pattern. KEY RECONCILIATIONS (epic text vs shipped reality): (1) epic L1370 `ControlUrl = parentService.ControlUrl` is WRONG-TYPED — ServiceDescription.ControlUrl is a possibly-RELATIVE string, SoapRequest.ControlUrl is an absolute Uri; the VM MUST resolve `new Uri(parentEntry.LocationUrl, parentService.ControlUrl)`. (2) epic L1359 "ShellViewModel.OpenInvocationPopupCommand(action)" is the WRONG SEAM — the shipped ActionNodeViewModel is a bare leaf holding only an ScpdAction with no back-reference to its service/entry/token; arch table L1330 says `ActionNodeViewModel.OnDoubleClick`. Correct design: enrich ActionNodeViewModel with parent context (ServiceDescription + RegistryEntry + NodeServices + deviceToken, threaded ServiceNode→ActionNode exactly like ServiceNode gets its context today) + an OpenInvocationPopupCommand that crosses the Core/App boundary via a NEW IInvocationPopupLauncher seam (mirror of Story 2.9's IPropertiesLauncher — Core interface, App impl that news the InvocationPopupWindow + Activate→Adopt). (3) device-removal reuses the 2.9 PATTERN EXACTLY: IDeviceRegistry.DeviceRemoved UUID-match → IsDeviceGone banner (notification) PLUS _popupCts linked to parentEntry.DeviceToken (in-flight abort) — D7's "two mechanisms, one outcome" (arch L833-837). So InvocationPopupViewModel ALSO injects IDeviceRegistry + is IDisposable (unsubscribe on close). (4) DUPLICATE SoapFault emit (epic L1409): 3.1 already emits Warning SoapFault INSIDE UpnpHttpClient with DeviceUuid=null; the popup-level emit is the UUID-bearing one — DECISION baked in: KEEP the popup emit (carries parentEntry.Uuid, the operator-facing identity), it is the FR-041-useful one; do NOT suppress. (5) IScpdParser is NOT needed in 3.2 (state-table parse is Story 3.3 — ArgumentInputViewModel is text-only base here, designed as the polymorphic seam for 3.3's AllowedValueList/AllowedValueRange subclasses). BLAST RADIUS: NodeServices gains a 7th member (IInvocationPopupLauncher) → breaks 4 test construction sites (DeviceNodeViewModelTests ×3, DeviceTreeViewModelTests ×1, ServiceNodeViewModelTests.MakeNodeServices — some target-typed `new(...)` so grep helpers too) + 1 DI reg + needs a FakeInvocationPopupLauncher (mirror FakePropertiesLauncher); ActionNodeViewModel ctor changes → updates ActionNodeViewModelTests (3 existing) + ServiceNodeViewModel.LoadActionsAsync ActionNode construction; App OnTreeDoubleTapped gains an ActionNodeViewModel branch. AUTOMATED SURFACE = Core InvocationPopupViewModel + ArgumentInputViewModel + InvocationResultViewModel ONLY (App popup window + launcher impl + window ownership are App-only, untestable — no App test project, CoreAppBoundary forbids Core.Tests→App). UI-TOUCHING → MANUAL UI SMOKE REQUIRED before review/done (Epic 2 retro action E + project memory) — first-class Task in the story, NOT deferred to epic close. ~18-22 new Core tests.
# 2026-06-03: Story 3.1 review → done by code-review workflow (claude-sonnet-4-6, bmad-code-review). APPROVED WITH MINOR FIXES. 3 patches applied by reviewer: (1) malformed-2xx body XmlException escape path — SoapResponseReader.ReadOutputArguments had no exception handling; added inner try/catch(XmlException) in InvokeActionAsync converting to UpnpProtocolException + Warning SoapInvoke diagnostic; (2) added regression test InvokeAction_HappyPath_MalformedResponseXml_ThrowsUpnpProtocolExceptionAndEmitsSoapInvoke [AC-3.1.4]; (3) added AC-3.1.5 trait to InvokeAction_Soap500WithFault and InvokeAction_Malformed500 tests (those tests now cover both AC-3.3 original and AC-3.1.5 new diagnostic assertions). 3 defers: $"unexpected status..." interpolated message (pre-existing from 1.3), UpnpFault public visibility (intentional), A9 synthetic-inner (flagged by dev). All 1.3 NFR invariants confirmed intact. Build 0/0 (Core), 1 pre-existing WMC1506. Suite 330 passed / 2 skipped / 0 failed (up from 329); chaos=1; CoreAppBoundary + DiagCategoriesUsage green. Dev declared deviations accepted: internal static classes (house style + InternalsVisibleTo), inline golden strings (correct), A9 not applied (non-blocking). Story 3.2 unblocked.
# 2026-06-03: Story 3.1 moved in-progress → review by dev-story workflow (claude-opus-4-8[1m]). SOAP envelope builder + fault parser + InvokeActionAsync re-wire. RESHAPE confirmed (not greenfield): reshaped SoapRequest (EnvelopeXml string → IReadOnlyList<SoapArgument> InputArguments) + SoapResponse (StatusCode/ResponseXml → ActionName + IReadOnlyList<SoapArgument> OutputArguments) + new SoapArgument record; relocated the trio Http/ → Models/ (namespace ohSpy.Core.Models, old Http/ files git rm'd). New Soap/ dir: SoapEnvelopeBuilder (XmlWriter, OmitXmlDeclaration — avoids UTF-16 decl trap, u: prefix on action elem, auto-escape, self-closing empty action FR-031), SoapFaultParser.TryParse(byte[],out UpnpFault) + UpnpFault data record (reuses shared UpnpXmlReaderSettings — XXE-locked), SoapResponseReader (dedicated class). Re-wired InvokeActionAsync: Build via SoapEnvelopeBuilder, 2xx → structured OutputArguments via reader, 500-parsable → NEW Warning SoapFault emit + UpnpFaultException, 500-unparsable + non-2xx/non-500 → SoapInvoke (was HttpTransport); timeout/cancel/HttpRequestException catches UNCHANGED. All 1.3 NFR invariants preserved (linked-CTS timeout, ResponseHeadersRead, size caps, quoted SOAPAction). Deleted inline TryParseUPnPError. Builder/parser/reader are internal static (InternalsVisibleTo); records public. Golden assertions = pinned inline strings (captured from real XmlWriter output). Found+fixed a reader double-advance bug during RED→GREEN (ReadElementContentAsString + while(Read) skipped every other arg). 16 new tests → 313→329 passed / 2 skipped (pre-existing intentional) / 0 failed; chaos=1; CoreAppBoundary + DiagCategoriesUsage green. Core build 0 warnings; App build 1 pre-existing benign WMC1506 (Story 2.5 FallbackTemplate, no XAML touched). A9 UpnpTransportException synthetic-inner fix NOT applied (non-blocking, left for a future PR that edits UpnpExceptions.cs). Pure Core, no UI surface → no manual smoke required (UI smoke discipline applies to 3.2). Recommend code-review (different LLM).
# 2026-06-03: Story 3.1 context created via bmad-create-story (claude-opus-4-8[1m]); Epic 3 opens (backlog → in-progress); 3-1 backlog → ready-for-dev. SOAP Envelope Builder + Fault Parser + InvokeActionAsync wire-up. ⚠️ KEY FINDING: this is a RESHAPE/REFACTOR, NOT greenfield — Story 1.3 front-ran a chunk. SoapRequest/SoapResponse ALREADY exist (in Http/, not Models/) with a pre-built EnvelopeXml-string / raw ResponseXml-string shape; InvokeActionAsync is ALREADY fully wired with an inline TryParseUPnPError. Story 3.1 must: (1) reshape SoapRequest (EnvelopeXml string → IReadOnlyList<SoapArgument> InputArguments) + SoapResponse (StatusCode/ResponseXml → string ActionName + IReadOnlyList<SoapArgument> OutputArguments), create new SoapArgument record; (2) relocate the trio Http/ → Models/ (namespace ohSpy.Core.Models) to match arch canonical tree (arch L2117-2122) + story AC; (3) add Soap/ dir: SoapEnvelopeBuilder (XmlWriter, OmitXmlDeclaration, u: prefix, XML-escape, empty-action FR-031), SoapFaultParser.TryParse(byte[],out UpnpFault) reusing UpnpXmlReaderSettings (Scpd, internal — same assembly), small SoapResponseReader; (4) re-wire InvokeActionAsync: Build via SoapEnvelopeBuilder, parse 2xx body into OutputArguments, replace inline TryParseUPnPError with SoapFaultParser, switch diag categories (500-parsable → NEW Warning SoapFault emit; 500-unparsable + non-2xx/non-500 → SoapInvoke, was HttpTransport; timeout stays HttpTimeout). DeviceUuid absent at http layer (SoapRequest carries no uuid) — 3.2 popup adds it. SoapInvoke/SoapFault DiagCategories already exist (pre-added). Blast radius: IUpnpHttpClient, UpnpHttpClient, StubUpnpHttpClient (+using), UpnpHttpClientTests (SampleSoap + happy-path assertions change). DiagCategoriesUsageTests is reflection-based → no change. Pure Core, NO UI surface → no manual smoke (UI smoke discipline applies to 3.2). Opportunistic: A9 UpnpTransportException synthetic-inner fix available (non-blocking). Golden-file builder tests + fault-parser fixtures (missing code/desc, malformed, XXE-attempt) are new.
# 2026-06-03: Epic 2 retrospective completed (epic-2-retrospective optional → done). Saved epic-2-retro-2026-06-03.md. Headline lesson: deferred manual smokes hid the device-tree expand bug (green VM tests set IsExpanded directly; UI never wired it + WinUI null container DataContext) through 2 stories + 4 reviews + 313 tests. New action items: E (smoke per UI-touching story before review/done — Project Lead decision), F (extend "trivially passing? prove it's wired" to UI bindings), G (record WinUI TreeView null-DataContext quirk as durable knowledge). FR-046 popup ownership (2.9) manually verified working → Epic 3's invocation popup reuse de-risked. Epic 3 unblocked; recommended start 3.1 (SOAP builder + InvokeActionAsync, pure Core).
# 2026-06-03: Epic 2 CLOSED (in-progress → done). All 9 stories (2.1–2.9) done + reviewed. Deferred manual UI smokes (2.6–2.9) RUN and PASSED on a live Sky Hub network. Manual smoke surfaced a real Story 2.5/2.6 device-tree bug (expand was a no-op: VM IsExpanded never wired + WinUI 3 TreeView leaves container DataContext null so {Binding Children} never resolved → no chevrons/children); fixed App-side (commit 4d380f8: code-assign container ItemsSource via LayoutUpdated + OnTreeExpanding lazy-load + OnTreeDoubleTapped) — chevrons + expand now work at every level. Core suite 313/2/0 unchanged. Open deferred-work items remain (notably Program.cs self-contained/bootstrap contradiction → clean-machine install blocker, scoped to Story 6.3). Next: epic-2-retrospective (optional) then Epic 3 (Action Invocation; Story 3.2 reuses the 2.9 WindowOwnershipManager + Activate→Adopt popup pattern).
# 2026-06-03: Story 2.9 moved review → done by code-review workflow (claude-sonnet-4-6, bmad-code-review run as sub-agent). APPROVED. Build 0 errors/0 warnings (clean; dev's "1 WMC1506" was incremental-build artifact). Tests 313 passed/2 skipped confirmed. Chaos=1, CoreAppBoundary=4 green. All AC-2.9.1..2.9.7 satisfied. 3 declared deviations all accepted: (1) GetChildrenOf returns IReadOnlyList<IntPtr> — pre-approved, avoids Window lifetime retention; (2) AllowUnsafeBlocks=true in ohSpy.App.csproj — required by [LibraryImport] SYSLIB1062, App-only, Core stays safe; (3) bool/Uri?→Visibility in PropertiesWindow code-behind not XAML converters — Window is not FrameworkElement so SetConverterLookupRoot(this) won't compile (CS1503), verified via generated .g.cs; correctly keeps Visibility out of Core. 0 patches required. 2 low/informational observations (LocationUri redundant TryResolve roundtrip; no dedicated LocationUri unit test) — both non-actionable, no production impact. Task 10 manual UI smoke (AC-2.9.3 FR-046 z-order/minimise/close + AC-2.9.5 render/hyperlinks + AC-2.9.6 live banner) not executed — headless env; recommend before Epic 2 close.
# 2026-06-03: Story 2.9 moved in-progress → review by dev-story workflow (claude-opus-4-8[1m]). 15 new PropertiesViewModel tests + retargeted 2.8 stub test → 313 passed / 2 skipped / 0 failed (was 298); chaos=1; CoreAppBoundary=4 green (confirms PropertiesViewModel + IPropertiesLauncher are WinUI-free). App build 0 errors, 1 pre-existing benign WMC1506 on the 2.5 FallbackTemplate (no new warnings). Delivered: WindowOwnershipManager (App/Windowing — [LibraryImport("user32.dll","SetWindowLongPtrW")] partial, GWLP_HWNDPARENT=-8, Dictionary<IntPtr,List<IntPtr>>+GetChildrenOf+Closed-pruning, canonical Activate()→Adopt() comment for Epics 3-5); IPropertiesLauncher Core seam + PropertiesLauncher App impl (Pattern-7 Func factory, dual-reg, ShellWindow set in App.OnLaunched); PropertiesViewModel (Core, ObservableObject+IDisposable, FR-052 snapshot at construction, OrDash absent→"—", FR-037 DeviceRemoved banner on UUID match, 2.8 BrowserLaunch hyperlink whitelist via OpenUrlCommand, relative-URL resolve vs LocationUrl, Interlocked dispose/unsubscribe); PropertiesWindow.xaml/.cs (5 FR-052 sections, read-only, ScrollViewer, banner, hyperlink-vs-"—" per URL). Deviations: (1) GetChildrenOf returns IReadOnlyList<IntPtr> (story-approved); (2) added <AllowUnsafeBlocks>true</AllowUnsafeBlocks> to ohSpy.App.csproj — required by [LibraryImport] SYSLIB1062 (App-only, Core stays safe); (3) bool/Uri→Visibility projections live in PropertiesWindow code-behind (App), not XAML x:Bind converters — a Window binding root is not a FrameworkElement so SetConverterLookupRoot won't compile (CS1503); still keeps Visibility out of Core. EmbeddedDevices always empty + placeholder (Decision 5, model flattens FR-053). DiagCategories UNCHANGED (Feature.NotImplemented still used by Subscribe stub); unused ohSpy.Core.Diagnostics using removed from DeviceNodeViewModel. NodeServices 6th member + 4 ctor sites + retargeted 2.8 stub test. Task 10 manual UI smoke (AC-2.9.3 FR-046 z-order/minimise/close, render/hyperlink/banner) NOT executed — headless env; recommend before Epic 2 close (joins 2.6/2.7/2.8 deferred smokes).
# 2026-06-03: Story 2.9 context created via bmad-create-story (claude-opus-4-8[1m]); backlog → ready-for-dev. Window Ownership Manager + Properties Window (first popup). Key design notes: (1) the "open a window" command crosses the Core/App boundary via a new IPropertiesLauncher seam (Core interface, PropertiesLauncher App impl) — DeviceNodeViewModel (Core) can't `new PropertiesWindow` (App/WinUI); the 2.8 OpenPropertiesCommand stub body is replaced with `_services.PropertiesLauncher.OpenProperties(_entry)`; (2) WindowOwnershipManager (App/Windowing) = [LibraryImport("user32.dll","SetWindowLongPtrW")] partial, GWLP_HWNDPARENT=-8, Dictionary<IntPtr,List<IntPtr>> + GetChildrenOf + Closed-pruning; canonical Activate()→Adopt() order is load-bearing (AC-10.1) + documented on Adopt for Epics 3-5; (3) ⚠️ WindowOwnershipManager/PropertiesLauncher/PropertiesWindow are App-only and CANNOT be unit-tested (no App test project; CoreAppBoundary forbids Core.Tests→App) — automated surface is PropertiesViewModel (Core) only; AC-10.2/10.3/10.4 + render are manual smoke (Task 10); (4) ⚠️ EmbeddedDevices section has NO data — DeviceDescription flattens embedded devices into Services (FR-053), retains no per-embedded records → render placeholder, documented gap (parser-model enhancement if wanted later); (5) hyperlinks (PresentationUrl/ManufacturerUrl/ModelUrl/LocationUrl) route through the Story 2.8 BrowserLaunch whitelist+shell-execute via OpenUrlCommand — NOT HyperlinkButton.NavigateUri (bypasses whitelist); relative URLs resolved against LocationUrl; (6) PropertiesViewModel (Core, partial ObservableObject, IDisposable) snapshots all display fields at construction (survives device removal — FR-037), subscribes to IDeviceRegistry.DeviceRemoved (UI-thread) → IsDeviceGone banner on UUID match, absent fields → "—" (OrDash); (7) Pattern 7 Func<RegistryEntry,PropertiesViewModel> factory (no IServiceProvider leak) + IWindowOwnershipManager singleton + PropertiesLauncher dual-reg (concrete+interface, DiscoveryService precedent); App.OnLaunched sets PropertiesLauncher.ShellWindow=_window; (8) IPropertiesLauncher → NodeServices 6th member → breaks 4 test ctor sites again (+FakePropertiesLauncher, FakeDeviceRegistry fakes) + retargets the 2.8 OpenProperties stub test. DiagCategories UNCHANGED (Feature.NotImplemented still used by Subscribe stub). ~14 new PropertiesViewModel tests (298→~313). Windowing+XAML+FR-046 are manual-verify (Task 10) — recommend before Epic 2 close (joins 2.6/2.7/2.8 deferred smokes).
# 2026-06-03: Story 2.8 moved review → done by code-review workflow (claude-sonnet-4-6, bmad-code-review). APPROVED. Build 0 errors/0 warnings (clean, confirmed). Tests 298 passed/2 skipped confirmed. Chaos=1, CoreAppBoundary=4 green. All AC-2.8.1..2.8.6 satisfied. 0 patches required. 1 low/informational observation (FakeUriLauncher.Launch records-before-throw design note — no production impact). Task 10.7 manual UI smoke (right-click UX + ≤2s open) remains pending before Epic 2 close.
# 2026-06-03: Story 2.8 moved in-progress → review by dev-story workflow (claude-opus-4-8[1m]). 15 new tests (283→298 passing, 2 skips unchanged), chaos=1, CoreAppBoundary=4 green. App build 0 errors (1 pre-existing benign WMC1506 on the 2.5 FallbackTemplate, shifted 121→140 by the two ContextFlyout blocks; no new warnings — new MenuFlyoutItem bindings are OneTime). IUriLauncher/ShellUriLauncher shell-open seam (Core, System.Diagnostics-only) + shared BrowserLaunch.OpenInDefaultBrowser helper (http/https whitelist Gap-3 + warn-on-refusal/failure, broad catch w/ documented CA1031 pragma); DeviceNodeViewModel.FetchXmlCommand (LocationUrl FR-019) + OpenPropertiesCommand stub (Feature.NotImplemented Warning — Story 2.9 fills body, Core VM can't open a Window); ServiceNodeViewModel.FetchServiceXmlCommand (new Uri(deviceLocation, ScpdUrl) FR-020) + SubscribeCommand stub ("subscribe not yet implemented" Warning + static "Subscribe (coming in Epic 4)" menu label — Story 4.1 relabels/removes); two new DiagCategories (Shell.Execute permanent, Feature.NotImplemented temp) + added to the pinned-set guard test; IUriLauncher → NodeServices 5th member (+1 DI reg + 4 test ctor sites + FakeUriLauncher); MainWindow.xaml device-Grid/service-StackPanel ContextFlyout+MenuFlyout x:Bind commands (Pattern 13). Sync void [RelayCommand]s (fire-and-forget AC-2.8.6). Task 10.7 manual UI smoke (right-click UX AC-2.8.1/2.8.4 + ≤2s open AC-2.8.2/2.8.6) NOT executed — headless env; recommend before Epic 2 close.
# 2026-06-03: Story 2.8 moved ready-for-dev → in-progress by dev-story workflow (claude-opus-4-8[1m]).
# 2026-06-03: Story 2.8 context created via bmad-create-story (claude-opus-4-8[1m]); backlog → ready-for-dev. Right-click context menus + XML viewing in default browser. Key design notes: (1) wrap the single Process.Start(UseShellExecute=true) behind a new Core IUriLauncher seam (ShellUriLauncher) so the Gap-3 whitelist + warn-on-failure are unit-testable — arch line 2187 maps shell-open to Process.Start, the seam IS that call behind an interface (project's INetworkInterfaceSource/IUiDispatcher seam philosophy); (2) shared BrowserLaunch.OpenInDefaultBrowser(url, launcher, diag, uuid) static helper — both device+service fetch commands run identical whitelist→launch→warn, do NOT duplicate; (3) commands are SYNC `[RelayCommand] void` not async Task — AC-2.8.6 fire-and-forget, no PostAsync readback (deviates from arch's generic async snippet); (4) service URL resolves `new Uri(_deviceLocation, _service.ScpdUrl)` reusing the Story 2.6 LoadActionsAsync idiom (relative SCPDURL common); (5) http/https whitelist via Uri.UriSchemeHttp/Https, other schemes → Warning(Shell.Execute)+skip; broad catch on launch failure with documented CA1031 pragma (FR-019 no-crash); (6) IUriLauncher added as NodeServices 5th member → breaks 4 test construction sites (+FakeUriLauncher fake) + 1 DI registration; (7) Properties… is a Core-side STUB Warning (Feature.NotImplemented) — real window in Story 2.9 (Core VM can't open a Window; 2.9 relocates/factory); (8) Subscribe stub Warning + static "Subscribe (coming in Epic 4)" menu label (chosen over transient flyout/feature-flag; 4.1 removes it); (9) MenuFlyout via ContextFlyout on device-Grid/service-StackPanel template roots, x:Bind OneTime to generated commands (Pattern 13, no Click= handlers). Two new DiagCategories (Shell.Execute permanent, Feature.NotImplemented temp). Right-click UX (AC-2.8.1/2.8.4) + ≤2s open (AC-2.8.2/2.8.6) are view-layer manual-verify (Task 10.7); all command logic unit-tested via Execute(null). ~13 new tests (283→~296).
# 2026-06-03: Story 2.7 moved review → done by code-review workflow (claude-sonnet-4-6, bmad-code-review). APPROVED. Build 0 errors/0 warnings (clean), 283 passed/2 skipped, chaos=1, CoreAppBoundary=4 green. All AC-2.7.1..2.7.7 satisfied. 1 deferred (OnLogEntriesChanged NewStartingIndex defensiveness). Task 9.5 manual UI smoke pending before Epic 2 close.
# 2026-06-03: Story 2.7 moved in-progress → review by dev-story workflow (claude-opus-4-8[1m]). 15 new tests (268→283 passing, 2 skips unchanged), App build 0 errors (1 pre-existing benign WMC1506 on the 2.5 FallbackTemplate, shifted 120→121 by the xmlns:m add; no new warnings), chaos unchanged (1), CoreAppBoundary 4 green. SsdpLogKind + SsdpLogEntry record (KindToken/TimestampDisplay/UuidText display members) + SsdpLogViewModel (NTS-only alive/byebye routing → BoundedObservableCollection(10_000) via IUiDispatcher.Post, IsAtTop, IDisposable, Clear()); ShellViewModel wiring (no new ctor args, no DI change, no existing-test edits); MainWindow right pane ItemsRepeater+ScrollViewer (FR-101) + smart auto-follow scroll handlers (FR-055, documented Pattern 13 exception). AC-2.7.7 Clear() forward-compat only (call site lands in Story 5.2). Task 9.5 manual UI smoke (AC-2.7.4/5/6 view behaviours) NOT executed — headless env; recommend before Epic 2 close.
# 2026-06-03: Story 2.7 moved ready-for-dev → in-progress by dev-story workflow (claude-opus-4-8[1m]).
# 2026-06-03: Story 2.7 context created via bmad-create-story (claude-opus-4-8[1m]); backlog → ready-for-dev. SSDP message log right pane: virtualised ItemsRepeater + smart auto-follow. Key design notes: (1) SsdpAnnouncement.Uuid is ALREADY parsed from USN — use `ann.Uuid ?? Guid.Empty`, do NOT re-parse (mirrors 2.6's AllServices→Services correction); (2) log routing is NTS-ONLY and narrower than DiscoveryService.RouteOnUiThread — absent-NTS M-SEARCH responses are NOT logged (FR-014/015 NOTIFY grammar), do not reuse `effectiveNt = NT ?? ST`; (3) AnnouncementReceived already fires on the UI thread but keep the IUiDispatcher.Post per AC + DiagnosticRingSink precedent (cheap FIFO re-queue, decouples VM); (4) timestamp stamped DateTime.UtcNow VM-side (event carries no arrival time, no clock seam in Core — DiagnosticEmitter precedent); (5) BoundedObservableCollection.PrependNewest already does FIFO eviction (Add(0)+Remove(10000), no Reset) — USE it, don't re-implement; (6) SsdpLogViewModel : IDisposable to unsubscribe (mirror DeviceTreeViewModel 2.5 review patch); (7) auto-follow is view-mechanics — documented Pattern 13 exception in MainWindow.xaml.cs (ScrollViewer.ViewChanged→IsAtTop; CollectionChanged→ChangeView), VM holds only IsAtTop; (8) bind computed KindToken/TimestampDisplay members on the record (OneTime), NOT fragile x:Bind ToString(). AC-2.7.7 (adapter-switch Clear) is forward-compat ONLY — method+test ship now, the call site lands in Story 5.2. AC-2.7.5/2.7.6 (scroll behaviours + chatty-burst perf) are view-layer manual-verify (Task 9.5), non-gating. ShellViewModel ctor unchanged (reuses existing discovery+ui args) → no test edits. ~14 new tests (268→~282).
# 2026-06-03: Story 2.6 moved review → done (committed a61ca08; Sonnet code-review APPROVED-WITH-MINOR-FINDINGS, both patches fixed + verified). F3 deferred (HTTP-layer cancellation test gap) + Task 12.5 manual UI smoke remain as epic-close items.
# 2026-06-03: Story 2.6 moved in-progress → review by dev-story workflow (claude-opus-4-8[1m]) after addressing code-review follow-ups. F1 (LOW) fixed: split fetch/parse try blocks so oversize-body UpnpProtocolException logs as ScpdFetch not ScpdParse, + regression test. F2 (cosmetic) fixed: removed dead `?? ""` on non-nullable ServiceType. Build 0 errors/0 warnings, 268 passed/2 skipped (was 267; +1 test), chaos=1. F3 remains deferred.
# 2026-06-03: Story 2.6 review → in-progress by code-review (claude-sonnet-4-6). APPROVED-WITH-MINOR-FINDINGS. 2 patches left as action items (F1: UpnpProtocolException from oversize HTTP body misclassified as ScpdParse — LOW; F2: dead null-check in ComputeLabel — COSMETIC). 1 deferred (AC-2.6.8 test only exercises parser-path OCE). Build 0 errors confirmed, 267 passed/2 skipped confirmed, chaos=1 confirmed, CoreAppBoundary 4 green. Spec deviation (CT last per CA1068/Pattern 6) ACCEPTED.
# 2026-06-03: Story 2.6 moved in-progress → review by dev-story workflow (claude-opus-4-8[1m]). 17 tests (250→267 passing, 2 skips unchanged), build 0 errors (1 pre-existing benign WMC1506 XAML warning on the 2.5 FallbackTemplate, no new warnings), chaos suite unchanged at 1, CoreAppBoundaryTests green. NodeServices bundle + ServiceNodeViewModel (lazy SCPD fetch + incremental action stream) + ActionNodeViewModel (leaf); DeviceNodeViewModel real expand handler (synchronous service-list build, Interlocked once-guard resolves the 2.5 ReplaceWith-Reset deferral); Service/Action XAML templates + selector routing. One spec deviation: ServiceNodeViewModel ctor takes CancellationToken last (CA1068 / Pattern 6). Task 12.5 manual UI smoke not executed (headless env).
# 2026-06-03: Story 2.6 moved ready-for-dev → in-progress by dev-story workflow (claude-opus-4-8[1m]).
# 2026-06-03: Story 2.6 context created via bmad-create-story (claude-opus-4-8[1m]); backlog → ready-for-dev. Service & action expansion: lazy SCPD fetch + incremental action stream. Key design notes: AC text says entry.Description.AllServices but the real model is DeviceDescription.Services (already flattened across embedded children by DeviceDescriptionParser — FR-053); deps threaded via a new DI-registered NodeServices record bundle (Http/ScpdParser/Ui/Diag) ShellVM→DeviceTreeVM→DeviceNodeVM→ServiceNodeVM (node VMs are `new`-constructed, not DI); SCPDURL resolved via new Uri(LocationUrl, ScpdUrl) (usually relative); incremental semantic streams into the BOUND Children (placeholder dropped on first action, subsequent actions appended — NOT buffer-then-swap); deviceToken (D7) passed to BOTH FetchScpdAsync + StreamActionsAsync so byebye cancels mid-parse (OCE swallowed, no diagnostic); Interlocked once-guards on device-services-build + service-actions-load resolve the Story 2.5 ReplaceWith-Reset deferral. Constructor changes to DeviceTreeViewModel + DeviceNodeViewModel require updating the existing 2.5 VM tests (Task 8). Fixture igd-router-200action.xml referenced in AC does NOT exist; streaming-order test uses linn-ds-5action.xml, 100-action perf is non-gating manual smoke.
# 2026-06-03: Story 2.5 moved review → done by code-review workflow (claude-opus-4-8). APPROVED-WITH-MINOR-FIXES. 5 patches applied (OnDeviceLoaded duplicate-UUID guard; ShellViewModel _runTask stored + awaited in DisposeAsync + IDiscoveryService drained; StartAsync Interlocked re-entrancy guard; DeviceTreeViewModel IDisposable unsubscribe; ComputeSecondaryDetail empty-tail + port<0 guards) + 3 regression tests (247→250 passing, 2 skips, chaos 1). KindGlyph "empty" finding was a FALSE POSITIVE — glyph U+E703 + FR-051 U+00B7 + Loading U+2026 were correctly UTF-8 all along; the mojibake signal was a Windows PowerShell 5.1 Get-Content ANSI-default artifact. 1 item deferred to Story 2.6 (ReplaceWith Reset collapses expansion).
# 2026-06-03: Story 2.5 moved in-progress → review by dev-story workflow (claude-sonnet-4-6). 18 tests (229→247 passing, 2 skips unchanged), build 0 errors (1 benign WMC1506 XAML warning), chaos suite unchanged at 1, CoreAppBoundaryTests green. CommunityToolkit.Mvvm 8.4.0 added; 7 ViewModels (NodeKind/INodeViewModel/placeholders/DeviceNodeViewModel/DeviceTreeViewModel/ShellViewModel); AdapterScope migrated from App.xaml.cs (Amendment A26); MainWindow shell with TreeView; MainPage deleted.
# 2026-06-03: Story 2.5 context created via bmad-create-story (claude-sonnet-4-6); backlog → ready-for-dev. Main window shell + DeviceTreeViewModel + ShellViewModel + AdapterScope migration (A26). Key design notes: CommunityToolkit.Mvvm 8.4.0 added to ohSpy.Core; ShellViewModel owns AdapterScope (moved from App.xaml.cs per A26); x:Bind requires typed ViewModel property on code-behind (not DataContext); NodeDataTemplateSelector needed for heterogeneous TreeView items; TreeView.ItemContainerStyle uses classic Binding (not x:Bind) for Children; DeviceUpdated uses Update(vm) not Remove+Add (FR-054 Move semantics).
# 2026-06-03: Story 2.4 moved review → done by Sonnet code-review (bmad-code-review). APPROVED-WITH-MINOR-FIXES. 1 patch: bare LF line endings normalised in SsdpParser (Replace CRLF→LF before split). 11 deferred items logged.
# 2026-06-02: Story 2.4 moved in-progress → review by dev-story workflow (claude-sonnet-4-6). 28 tests (201→229 passing, +2 skips unchanged), build 0 warnings, chaos suite unchanged at 1. SsdpAnnouncement + SsdpParser + IDiscoveryService/DiscoveryService + DI + App startup wired.
# 2026-06-02: Story 2.4 moved ready-for-dev → in-progress by dev-story workflow (claude-sonnet-4-6).
# 2026-06-02: Story 2.4 context created via bmad-create-story (claude-sonnet-4-6); backlog → ready-for-dev. SsdpParser + DiscoveryService + ChannelSsdpTransport fake. Key design notes: M-SEARCH responses use ST not NT (effectiveNt = ann.NT ?? ann.ST for routing); DiscoveryService must start AFTER scope.StartAsync(); adapterToken threaded into OnAlive for D7 device CTS; FakeSsdpTransport (cap=1) NOT suitable for DiscoveryService integration tests — ChannelSsdpTransport (cap=256) created instead.
# 2026-06-02: Story 2.3 moved review → done by Sonnet code-review (bmad-code-review). APPROVED-WITH-MINOR-FIXES. 2 patches: DeviceCts.Dispose() in RemoveCore + DeviceToken snapshotted at construction; MarkInFlight Post guarded against cancelled token.
# 2026-06-02: Story 2.3 moved in-progress → review by dev-story workflow (claude-opus-4-8[1m]). 36 tests (165→199 passing, +2 skips), build 0 warnings, chaos suite unchanged at 1, DI graph cycle-free. Also fixed a pre-existing flaky allocation test (process-wide → thread-local GC). A27/A28 (D9 stale snippets) + A29 (allocation-test robustness) amendment candidates raised.
# 2026-06-02: Story 2.3 moved ready-for-dev → in-progress by dev-story workflow (claude-opus-4-8[1m]).
# 2026-06-02: Story 2.3 context created via bmad-create-story (claude-opus-4-8[1m]); backlog → ready-for-dev. Device registry + DescriptionFetchState machine + EagerDescriptionDispatcher. Key design notes: registry backing store MUST be ConcurrentDictionary (identity-lookup reads off the emitting thread per DiagnosticRingSink.Push); DeviceRegistry must NOT depend on IDiagnosticEmitter (DI cycle); registry↔dispatcher cycle broken via internal EntryNeedsFetch event; DeviceDescription.Udn is a string ("uuid:<guid>") not Guid RootUdn → UdnMatches normalisation. A27/A28 amendment candidates raised (D9 stale snippets).
# 2026-06-02: Story 2.2 moved review → done by Sonnet code-review (bmad-code-review). APPROVED-WITH-MINOR-FIXES. 5 patches applied: StartAdapterScopeAsync wrapper, D7 shutdown ordering, volatile _transportStarted, CurrentAdapterIPv4 post-bind, FakeSsdpTransport.TeardownCts.
# 2026-06-02: Story 2.2 moved in-progress → review by dev-story workflow (claude-opus-4-8[1m]). 19 tests (144→163 passing, +2 skips), build 0 warnings, chaos suite unchanged at 1. A26 amendment candidate raised (App-level disposable ownership / CA1001); A23 restated as 5.2 prerequisite (transport factory).
# 2026-06-02: Story 2.2 moved ready-for-dev → in-progress by dev-story workflow (claude-opus-4-8[1m]).
# 2026-06-02: Story 2.2 context created via bmad-create-story (claude-opus-4-8[1m]); backlog → ready-for-dev. Network adapter enumerator + AdapterScope + startup bind. Key design notes: INetworkInterfaceSource/AdapterCandidate testability seam (NetworkInterface is unmockable); App.OnLaunched is interim orchestrator (2.5 relocates AdapterScope into ShellViewModel); transport stays 2.1 singleton (A23 factory deferred to 5.2 switch).
# 2026-06-02: Story 2.1 moved review → done by Sonnet code-review (bmad-code-review). APPROVED-WITH-MINOR-FIXES. 5 patches applied: search-socket leak, overflow telemetry pre-write, AC-2.1.5 tests, AC-2.1.4 search-response test, TOCTOU fix.
# 2026-06-02: Story 2.1 moved in-progress → review by dev-story workflow (claude-opus-4-8[1m]). 16 tests, build 0 warnings. A22 amendment candidate raised.
# 2026-06-02: Story 2.1 moved ready-for-dev → in-progress by dev-story workflow (claude-opus-4-8[1m]).
# 2026-06-02: Story 2.1 context created via bmad-create-story; Epic 2 opens (backlog → in-progress); 2-1 backlog → ready-for-dev.
# 2026-06-02: Story 1.6 moved review → done by Sonnet code-review (bmad-code-review). APPROVED-WITH-MINOR-FIXES.
# 2026-06-02: Story 1.5 moved ready-for-dev → in-progress → review by dev-story workflow (claude-opus-4-7[1m]).
# 2026-06-02: Story 1.5 moved review → done by Sonnet code-review (bmad-code-review). APPROVED-WITH-MINOR-FIXES.
# 2026-06-01: Story 1.1 moved ready-for-dev → in-progress → review by dev-story workflow.
# 2026-06-01: Story 1.1 moved review → done by Sonnet code-review (bmad-code-review).
# 2026-06-02: Story 1.2 context created via bmad-create-story; backlog → ready-for-dev.
# 2026-06-02: Story 1.2 moved ready-for-dev → in-progress → review by dev-story workflow (claude-opus-4-7[1m]).
# 2026-06-02: Story 1.2 moved review → done by Sonnet code-review (bmad-code-review). APPROVED.
# 2026-06-02: Story 1.3 context created via bmad-create-story; backlog → ready-for-dev.
# 2026-06-02: Story 1.3 moved ready-for-dev → in-progress → review by dev-story workflow (claude-opus-4-7[1m]).
# 2026-06-02: Story 1.3 moved review → done by Sonnet code-review (bmad-code-review). APPROVED-WITH-MINOR-FIXES.
# 2026-06-02: Story 1.4 context created via bmad-create-story; backlog → ready-for-dev.
# 2026-06-02: Story 1.4 moved ready-for-dev → in-progress → review by dev-story workflow (claude-opus-4-7[1m]).
# 2026-06-02: Story 1.4 moved review → done by Sonnet code-review (bmad-code-review). APPROVED.
# 2026-06-02: Story 1.5 context created via bmad-create-story; backlog → ready-for-dev.
# 2026-06-02: Story 1.6 context created via bmad-create-story; backlog → ready-for-dev.
# 2026-06-02: Story 1.6 moved ready-for-dev → in-progress → review by dev-story workflow (claude-opus-4-7[1m]).
# 2026-06-02: Story 1.6 moved review → done by Sonnet code-review (bmad-code-review). APPROVED-WITH-MINOR-FIXES.
# 2026-06-02: Epic 1 CLOSED — all 6 stories done. A16 + A18 architecture amendments applied.
# 2026-06-02: Story 2.1 context created via bmad-create-story; backlog → ready-for-dev. Epic 2 opens: backlog → in-progress.
project: ohSpy
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # Epic 1 — Project Foundation & Test Infrastructure (CLOSED 2026-06-02)
  epic-1: done
  1-1-project-scaffold-build-test-installer-pipeline: done
  1-2-ui-dispatcher-contract-collection-primitives: done
  1-3-upnp-http-client-facade-with-per-request-timeout-discipline: done
  1-4-xml-parsers-scpd-streaming-device-description-with-xxe-defence: done
  1-5-diagnostic-emitter-ring-sink-file-sink: done
  1-6-fakeupnpdevice-minimal-modes-first-chaos-test-netarchtest-rules: done
  epic-1-retrospective: done

  # Epic 2 — Device Discovery & Tree Browsing (CLOSED 2026-06-03)
  epic-2: done
  2-1-ssdp-transport-multicast-search-sockets-with-bounded-channel: done
  2-2-network-adapter-enumerator-adapter-scope-startup-bind: done
  2-3-device-registry-descriptionfetchstate-machine-eager-description-dispatcher: done
  2-4-ssdp-parser-discoveryservice-wire-transport-into-registry: done
  2-5-main-window-shell-device-tree-top-level-rows: done
  2-6-service-action-expansion-lazy-scpd-incremental: done
  2-7-ssdp-message-log-right-pane-virtualised-smart-auto-follow: done
  2-8-right-click-context-menus-xml-viewing-in-default-browser: done
  2-9-window-ownership-manager-properties-window-first-popup: done
  epic-2-retrospective: done

  # Epic 3 — Action Invocation (CLOSED 2026-06-04)
  epic-3: done
  3-1-soap-envelope-builder-fault-parser-and-invokeactionasync-wire-up: done
  3-2-invocation-popup-with-free-form-text-inputs: done
  3-3-constrained-inputs-allowedvaluelist-dropdown-allowedvaluerange-numeric: done
  epic-3-retrospective: done

  # Epic 4 — GENA Subscription
  # 2026-06-04: Story 4.1 code-review → done (claude-sonnet-4-6, bmad-code-review). APPROVED-WITH-MINOR-FIXES. Verdict: the first inbound network listener is architecturally sound and all five hardening controls hold. Build: Core 0/0, App 1 pre-existing WMC1506. Tests: 443 passed / 2 skipped / 0 failed (post-patch, same count). CONTROLS: (1) Bind target — confirmed TcpListener(IPEndPoint(adapterIPv4, 0)), NOT IPAddress.Any; test asserts CallbackBaseUrl.Host != "0.0.0.0". PASS. (2) Connection cap — SemaphoreSlim(8,8) Wait(0) non-blocking gate; 9th connection accepted-then-immediately-closed + GenaCallbackFlood; slot always released in finally (even on throw) — FloodTest (50 conns → ≤8 in-flight confirmed). PASS. (3) Slowloris/TimeoutStream — per-read CTS linked to caller token; caller-cancel → OCE (shutdown, swallowed); budget-fire → CallbackTimeoutException (sentinel); shutdown wins over timeout correctly. Idle-time model: per-read reset, not wall-clock cap. Drip test at 150ms budget + 300ms gap fires HeadersTo; 9th connection served after slots free. PASS. (4) Oversize body — rejected by Content-Length BEFORE buffering (test sends zero body bytes with CL=2MB → 413). 16KB header cap enforced incrementally (per-byte accumulator check). PASS. (5) Framing — CL+TE both → 400 (TE checked first); duplicate CL → 400 (strict, not last-wins); bare CR → 400; bare LF accepted; three-SP → 400; lowercase method → 400; obsolete-fold → 400; chunked → 400; missing CL → 411. PASS. 3 PATCHES APPLIED: (a) _slots.Release() wrapped in try/catch(ObjectDisposedException) — after drain-timeout force-close, disposed semaphore would cause unobserved task fault [EventCallbackHost.cs:155]; (b) drain-overrun Warning now passes new DiagnosticContext() for Pattern 11 structural consistency — RemoteEndpoint is null by design for host-level event [EventCallbackHost.cs:393]; (c) drain-overrun test adds explicit assertion documenting the null-RemoteEndpoint exception [EventCallbackHostTests.cs:500]. OPEN Q#1 RESOLVED: GenaCallbackMalformed for 500/dispatch-error and GenaCallbackFlood for drain-overrun both APPROVED — message/ErrorText fields distinguish them in logs; adding new constants would touch DiagCategoriesUsageTests pinned-set guard (not worth it for two rare paths). DEFERRED: lying CL test (safe by design, no keep-alive); EOF-as-body-stall semantic mislabelling (intentional design); semaphore slot TOCTOU on pre-cancelled token (harmless on shutdown). Story 4.2 unblocked.
# 2026-06-04 (Epic 3 retro decision): Story 5.2 (adapter switch) RE-SEQUENCED from Epic 5 into
  # Epic 4, placed LAST. Rationale: 5.2's atomic-rebind sequence is specified to DisposeAsync/
  # StartAsync the EventCallbackHost (Story 4.1) and notify the subscription popup (Story 4.3) —
  # a one-way forward dependency, so 5.2 must run AFTER 4.1/4.3 (no reverse dependency: Epic 4
  # doesn't need adapter switching). Keeps key "5-2-…" to avoid breaking architecture A23 / FR-050 /
  # epic cross-refs. Prerequisite: A23 transport-factory refactor (transport is still a 2.1 singleton;
  # rebind needs per-adapter dispose+reconstruct). Epic 4 PREP ITEM (kickoff): add a small reversible
  # dev adapter override (env var OHSPY_ADAPTER=name|index, startup bind) so the Linn-DS network is
  # reachable to smoke Epic 4 eventing + the deferred 3.2 (steps 5/6/7) and 3.3 (steps 2/3/5) UI paths
  # BEFORE 5.2 lands. See epic-3-retro-2026-06-04.md.
  epic-4: in-progress
  4-1-event-callback-host-tcplistener-hand-rolled-http-1-1-parser: done
  4-2-subscription-client-subscribe-renew-unsubscribe-lifecycle-with-auto-renewal: done  # code-review 2026-06-04 APPROVED-WITH-MINOR-FIXES → both patches APPLIED (Opus): CloseAsync awaits renew loop + NOTIFY worker (bounded AwaitBoundedAsync — no post-close RaiseNotification, protects 4.3's _ui.Post handler) + wasLapsed re-read after drain (TOCTOU near-zero). Core -warnaserror 0/0; 462 passed/2 skipped; SubscriptionClient suite green 6×. 2 deferred (_adapterToken barrier, no DisposeAsync) → Story 5.2.
  4-3-subscription-popup-event-list-latest-property-values-multiple-concurrent-popups: done  # code-review APPROVED-WITH-MINOR-FIXES (Sonnet) + follow-ups handled (487 passed); manual event-stream smoke ACCEPTED-AS-DEFERRED by Project Lead 2026-06-04 (no event-emitting device reachable w/o Action-I override) → logged deferred-work.md, run at Action-I/Story 5.2. dev-story 2026-06-04 (Opus): Core SubscriptionPopupViewModel + App SubscriptionPopupWindow/Launcher. ⚠️ ALL handle event/replay mutations marshalled via _ui.Post, guarded by 4 DeferredUiDispatcher tests (no real-thread reliance). Core -warnaserror 0/0; 485 passed/2 skipped (+23 Core tests); chaos 1; CoreAppBoundary+DiagCategories pinned-set green; App build 1 pre-existing benign WMC1506 only. 2.8 Subscribe stub removed (DiagCategories.FeatureNotImplemented now prod-unused but PINNED → left in place per §6, flagged to reviewer). MANUAL UI SMOKE (Task 12) GATE OPEN — headless + depends on not-yet-built Action-I OHSPY_ADAPTER override to reach an event-emitting Linn DS; Core VM unit-tested as compensating control.
  5-2-adapter-switch-view-network-adapter-menu-atomic-rebind: review  # dev-story 2026-06-04 (Opus, claude-opus-4-8[1m]). A23 transport+host factories (Func<ISsdpTransport>/Func<IEventCallbackHost>; host CANNOT re-start after dispose → factory + re-thread ISubscriptionClient.SetCallbackHost); DiscoveryService shape (b) singleton+RebindAsync(reader) keeps SsdpLogVM subscription stable; DeviceRegistry.Clear() created; ShellViewModel.SwitchAdapterAsync 10-step atomic rebind (re-entrancy guard, 2s budget, AdapterSwitchTimeout on transport-teardown overrun, marshalled transient, reuse StartBoundServicesAsync); View Button+MenuFlyout (MenuFlyoutSubItem has no Opening → title-bar flyout populate-on-Opening, RadioMenuFlyoutItem per adapter / disabled single item). Core -warnaserror 0/0; full suite 503 passed/2 skipped (+16: ShellViewModelTests 11, AdapterSwitchPopupCascadeTests 2, DeviceRegistry Clear 3); chaos 1; CoreAppBoundary+AsyncDiscipline+DiagCategoriesUsage green; NO new DiagCategories constant; App build 1 pre-existing WMC1506 (shifted 141→156 by the menu, same element, no new warning). ⚠️ MANUAL UI SMOKE (Task 6) GATE OPEN — headless, needs two real adapters + Linn-DS network; it is the VERIFICATION KEYSTONE bundling deferred 4.3 event-stream + 3.2(5/6/7) + 3.3(2/3/5); SUPERSEDES retro Action-I. NOT committed.
  epic-4-retrospective: optional

  # Epic 5 — Operator Tooling — Diagnostics, Rescan  (5.2 adapter-switch re-sequenced into Epic 4, see above)
  epic-5: backlog
  5-1-diagnostics-viewer-window: backlog
  5-3-rescan-view-rescan-menu-prune-non-responders: backlog
  epic-5-retrospective: optional

  # Epic 6 — Polish, Soak & Release Readiness
  epic-6: backlog
  6-1-manual-ui-verification-fr-044-fr-046-fr-054-behaviours: backlog
  6-2-soak-tests-30-min-no-crash-8-hour-scale-ceiling: backlog
  6-3-performance-budget-verification-clean-machine-install-dry-run: backlog
  epic-6-retrospective: optional
