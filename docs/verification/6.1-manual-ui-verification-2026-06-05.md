# Story 6.1 — Manual UI Verification Report

- **Date:** 2026-06-05
- **Verifier:** Simon Chisholm (Project Lead)
- **Build / commit:** `54f22df` (confirm against the build you run — `git rev-parse --short HEAD`)
- **Adapter / network:** _<adapter name, IPv4, LAN description — set via View → Network adapter>_

> Scaffold prepared 2026-06-05. Fill in the **Result** + **Evidence** columns as you walk the checklist
> (story `6-1-...`, Tasks 0–7). PASS / FAIL / N/A per AC. Anything that FAILs becomes a Task-8 minimal fix.

## Test devices

_Record the inventory you ran against (Task 0)._

| Friendly name | deviceType tail | host:port | Role in verification |
|---|---|---|---|
| _e.g. Linn Kitchen_ | `MediaRenderer:1` | `192.168.0.x:nnnn` | large OpenHome (≥ 20 services) |
| _e.g. router_ | `WANConnectionDevice:1` | `192.168.0.1:nnnn` | IGD — 100+-action SCPD |
| _..._ | | | event-emitter (Ds/Product/Volume) |

## AC results

| AC | Behaviour | Result | Evidence / notes |
|----|-----------|--------|------------------|
| 6.1.1  | Row composition (glyph + name + muted secondary line)       | _PASS_ | screenshot |
| 6.1.2  | No flicker on re-announce (NFR-UI3)                          | _PASS_ | |
| 6.1.3  | Persistent chevron before any click (FR-044/AC-A1.1)        | _PASS_ | |
| 6.1.4  | "Loading…" placeholder during service load                  | _PASS/FAIL/N/A_ | |
| 6.1.5  | Chevron no flicker during load — **large (≥20-svc) device**  | _PASS_ | regression watch `4d380f8` / `a55ed74` |
| 6.1.6  | Incremental SCPD stream, no freeze, no chevron collapse      | _PASS_ | |
| 6.1.7  | Cold large-SCPD expand ≤ 2 s (100+-action SCPD)             | _PASS_ | **measured: ___ s** |
| 6.1.8  | Action node = leaf (no chevron)                             | **PASS** | re-verified after the D1 fix |
| 6.1.9  | In-place row migration preserves selection/expansion/scroll | **N/A** | a friendlyName change in practice requires the device to leave (byebye) then rejoin (alive) — it is NOT an in-place metadata update — so the FR-054 migration path isn't reachable on real hardware |
| 6.1.10 | All four popups open-on-top (AC-10.1 retained)              | **PASS** | all four open on top; the Invocation (double-click) popup-behind defect (D2) fixed + re-verified |
| 6.1.11 | **A31 free z-order** — clicking main brings it forward       | _PASS_ | A31 NEW behaviour (was AC-10.4) |
| 6.1.12 | **A31 independent minimise** — popups don't minimise w/ shell| _PASS_    | **SUPERSEDED by A31** (old AC-10.3) |
| 6.1.13 | Close-with-parent — closing main closes all popups (AC-10.2) | _PASS_ | no exception / no taskbar orphan |
| 6.1.14 | SSDP burst (≥20 adv/s, ≥30 s) — no dropped frames           | _N/A (deferred)_ | needs a busier network — re-test later (with 6.1.4) |
| 6.1.15 | Smart auto-follow respected (FR-055)                        | **PASS** | scroll-away no-yank; scroll-top re-engages |
| 6.1.16 | WinUI 3 conformance ("considered", not pixel-perfect)       | **PASS** | deliberate dev-dense deviations (11px muted secondary, Consolas log) noted as conscious |

### WinUI render-hazard watch (the defect classes 6.1 exists to catch)

While walking the above, specifically watch for these four (a recurrence = Task-8 fix):
1. **Chevron missing / "Loading…" stuck** on a large device (`winui-treeview-datacontext-null`) — covered by 6.1.3/6.1.5.
2. **Subscription popup property rows** render without crash (`winui-no-struct-databinding`) — open a Subscribe popup on an event emitter.
3. **No `RPC_E_WRONGTHREAD` crash** under live NOTIFY on the Subscription + Diagnostics popups (`winui-no-synccontext-marshal-vm`).
4. **Diagnostics severity colours + SSDP log rows** actually render (not all-default) (`winui-window-root-itemsrepeater-binding`) — the Story 5.1 P1 class.

## Amendment A31 note

AC-10.3 (minimise/restore-together) and AC-10.4 (no-push-behind) from the original FR-046 / Decision-10
spec are **SUPERSEDED by Amendment A31** (commit `5668759`) — popups float in free z-order; no Win32
owner link. The app exhibits the deliberate post-A31 behaviour: a popup opens on top (`Activate()`), then
floats in normal z-order (clicking the shell brings it forward), does **not** minimise with the shell, and
**does** close when the shell closes (explicit `parent.Closed` handler). Recorded N/A / re-specified above —
this is correct, not a defect.

## Defects found & resolutions

| # | AC | Description | Resolution (commit / deferred) |
|---|----|-------------|-------------------------------|
| D1 | 6.1.8 | Phantom expand chevron on action (leaf) rows after expanding then collapsing a many-service device (e.g. OpenHome `Source:1`). WinUI recycles `TreeViewItem` containers; one reused from a service row kept its `ItemsSource` → stale chevron on the action. | Fixed in `MainWindow.xaml.cs` `AssignContainerSources` (clears a recycled leaf container's `ItemsSource`). **RE-VERIFIED PASS.** |
| D2 | 6.1.10 | Invocation popup (opened by double-clicking an action) flashes to front then drops behind the main window — the double-click's mouse-up re-focuses the shell after the popup `Activate()`, and post-A31 (no owner link) the popup floats behind. | 1st attempt (deferring the open one tick in `OnTreeDoubleTapped`) was INSUFFICIENT — the shell still reclaimed foreground after. Re-fixed in `InvocationPopupLauncher`: a **Low-priority** deferred re-`Activate()` runs once the double-click input fully unwinds, re-asserting the popup on top. **RE-VERIFIED PASS.** |
| D3 | 6.1.4 | "Loading…" placeholder couldn't be observed — all devices load too fast on this network. | **Deferred** — re-test on a busier/slower network (with 6.1.14 SSDP burst). |

## Conclusion

**PASS for the L&L demo.** All observable ACs passed. Two polish defects found during the walkthrough
were fixed under this story and re-verified on the live network: **D1** (phantom expand chevron on
recycled action rows) and **D2** (Invocation popup dropping behind the shell on double-click, post-A31).

**Deferred to a busier-network session** (could not be exercised on this LAN — devices load too fast / not
enough SSDP traffic), tracked in `deferred-work.md`:
- **6.1.4** "Loading…" placeholder visibility.
- **6.1.14** SSDP burst (≥ 20 adv/s for ≥ 30 s) — no dropped frames.

**6.1.9** marked **N/A** (a runtime friendlyName change reaches us as a byebye→alive cycle, i.e. Remove+Add,
not an in-place metadata Move — the FR-054 in-place-migration path isn't reachable on real hardware; the
Move-not-Remove+Add invariant is covered by `IdentityKeyedSortedCollection` unit tests). **AC-10.3 / AC-10.4**
recorded N/A / re-specified per the Amendment A31 note above (deliberate post-A31 behaviour, not defects).
